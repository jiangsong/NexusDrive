# 剩余模块开发与验收

目标：完成当前项目未完成模块与 bug 修复的开发和测试。以实际代码、测试和运行记录判定完成，原 TODO 中的旧状态仅作线索。

## 工作清单

- [x] 上传内容证明：将 115 区间签名、阿里证明字节接入通用上传链路，拒绝越界和短读。
- [x] Journal：修正并发组提交验收的时序假设，验证单写者没有聚合等待；目录 fsync 失败不确认提交。
- [~] 账号与凭据：安全存储、轮换持久化、config add/auth/list 和授权流程。
- 已实现：keyring/0600 文件引用、YAML 原子更新、阿里/百度/115 轮换保存、配置保护、导入/迁移/检查和初始授权命令；真实账号、终端输入/二维码、系统 keyring 和交互式公开字段向导仍待验收/完善。
- [~] 控制面：Unix socket、状态与管理调用、陈旧 socket 恢复。
- 已实现：Unix socket 与 TCP 双监听、0600 权限、锁保护的陈旧 socket 清理、在线 status、上传 list/retry/flush/cancel/resume/drop 与只读离线日志，以及在线 pin/unpin/warm/cache 管理；完整终态历史回收仍待补。
- 上传清理：journal v11 包含 v10 的 purging 意图、共享内容保护、故障重试与 ID 防复活，并新增普通上传的原元数据库/挂载/账号绑定；元数据本地别名移除/FULL 屏障、VFS 句柄/挂载与缓存协调、daemon 启动续清理已接入。CLI/control 的在线及离线统一确认入口已有真实命令强杀恢复回归；首次已替换/缺失的历史版本、完整历史回收和规模/真实环境验收仍开放，详见 `upload-cleanup.md`。
- [~] 复制：VFS 服务端复制及跨盘缓存复用、CLI/控制面/MCP 复制入口与准备任务查询/重试/取消/清理、准备及发布恢复已有回归。准备任务的源/目标账号代次及已交接上传目标绑定均已接入；远端对账、并发无覆盖契约、准备目标改名和完整终态历史仍待补，见 `copy.md`。
- [~] MCP Resources 与管理：列举/读取/有界缓存分页、冷刷新 TEMP 分批暂存与原子合并、WebDAV/OpenList 与生产 SFTP 流式枚举、独立 VFS 事件流、新旧协议订阅、权限过滤、通知合并和断连清理已接入；复制准备及上传查询/retry/cancel/resume/discard/flush 工具已接入。其他全量入口、暂存空间与大事务性能、慢连接通知隔离、SDK 长期回收及真实环境验收待补。
- [~] 目录一致性：列表和 delta 的目录替换/后代保护、事务快照检查、刷新代次及活动写句柄保护已有回归；远端父目录重建、跨客户端快照与任意祖先/后代并发的完整一致性仍不能据此宣称完成。
- [~] 搜索：路径片段搜索、短查询索引与改名一致性已有回归；大量 pending、宽查询及真实规模长期性能待补。
- [~] 缓存与 FUSE：pin/unpin、规则恢复、缺块补齐、完整文件预算/GC、日志空间背压、共享 backing 租约已有；passthrough 读写模式/版本切换、writeback/splice 能力核验和内核验收待补。实验 passthrough 暂不默认启用，不视为该模块完成。
- [~] 国外网盘/协议：S3、Dropbox、OneDrive 已接入；GDrive、Box、SMB 仍缺。
- [~] 运行与分发：systemd/launchd 用户服务、Docker/Compose、双平台 race CI、tag 静态附件/checksum 与 GHCR 多架构发布已接入；Docker daemon、GitHub tag、服务重启及包管理器真机验收仍缺。
- [~] 产品入口：回环 control 状态/上传管理页、只读 WebDAV VFS 子树、直链策略与 STRM 生成已接入；Web 账号授权、可写 DAV、媒体调优与真实媒体流程仍缺。
- [~] 验证：已有全量测试/race/vet 与交叉构建记录（见各批次）；真实账号、macOS 挂载、Linux 特权挂载、物理掉电、性能与 POSIX 验收未齐。
- [ ] 文档：逐条校准 TODO/README/设计与实际实现；许可证由项目所有者选择。

Windows/WinFsp、FSKit 按原计划为后续范围。缺少真实账号或平台环境时记录未验收，不以模拟测试替代真实验收。

## 起始证据

2026-09-05，macOS arm64：全量测试中 journal 两个性能/调度断言失败；组提交测试三次独立复跑均失败。组提交测试在逐个同步落盘 staging 的同时启动提交，没有保证提交者同时到达。真实 FUSE 测试因未安装 macFUSE/Fuse-T 跳过。

代码核对：115 区间签名和轮换 token 的持久化尚未接入；上传成功时先登记缓存再更新节点的修复已经存在；passthrough 已实现。国内驱动仍有 51 处非测试 `UNVERIFIED:`，需要真实账号核对。

## 2026-09-05 第一批实现

- `provider.UploadRangeHasher` 由 uploader 根据本次 staging 内容注入，115 使用同一上下文契约，驱动包装层不丢能力。测试覆盖实际 115 HTTP 握手经过 journal/uploader 的路径，以及越界、截断、取消。
- Journal 组提交测试先完成 staging 再同时提交，避免把磁盘延迟误当并发；单提交检查实际聚合窗口计数。目录打开/fsync 失败向所有提交者报错，恢复存储后可重试。
- `control.Server.Start` 同步绑定端点，启动失败不会静默丢失控制面。Unix socket 用持久锁文件协调恢复，拒绝覆盖普通文件和活动 socket。`status` 优先连接运行中的 daemon，离线日志通过 SQLite `mode=ro` 打开。
- `secrets.backend: auto|keyring|file`，独立文件权限 0600，YAML 使用 `keyring:` 或 `secretfile:` 引用。依赖来源：[go-keyring](https://github.com/zalando/go-keyring)。测试使用替代 keyring 函数，不操作用户真实 keychain。
- token 保存失败保留新 token 并重试保存，不在保存失败时继续暴露新凭据；daemon 级两次启动的 HTTP 回放验证了轮换值的落盘和重载。
- 全库 `go test -race ./... -timeout 120s` 与 `go vet ./...` 已通过（目录 fsync 最新变更另做三轮 journal race 和相关回归）。FUSE 依赖的测试仍因环境缺失跳过；未把它们计为真机验收。

## 下一步

账号命令主体、pin/unpin、完整文件计量/GC 和日志空间背压已接入，继续推进 passthrough 生命周期、Copy、MCP Resources、搜索与其余清单。在线丢弃上传必须同时处理 VFS 的本地版本、打开句柄与上传竞争，不能直接删除 journal 行；离线 drop 后本地残留版本的清理也需一起验收。真实账号、系统 keyring 与内核挂载缺口保持开放。

## 2026-09-05 第二批：上传排空与管理接线

- 本轮重新跑全库发现 `TestNetworkDropDuringUploadRecovers` 失败；单项复跑 10 次失败 9 次。根因是测试把“处理当前到期任务”的 `DrainAll` 当成“等待全部上传结束”，没有等退避到期。
- 新增 `Uploader.Flush`：等待 pending（含延迟重试）与 uploading 清空；不越过退避、不自动重试死信；最终仍有死信则返回错误。已启动 worker 时只观察现有池，不增加并发；未启动时同步处理。调用方 context 控制期限，成功只保证返回时已提交队列的快照，不包括仍打开的文件句柄。
- 取消传输后用独立、有期限的 context 将任务退回 pending，不消费失败次数。修正 Start/Stop 间 WaitGroup 注册的竞争窗口。
- `GET /uploads` 按 ID 游标分页（默认 200，最大 1000）；`POST /uploads/retry` 与 `/uploads/flush` 接入持有队列的 daemon。响应不包含 blob 路径、hash 和上传 session。变更请求要求专用头，拒绝浏览器 Origin 和非本地主机名。
- CLI `uploads list/retry/flush` 优先使用控制端点。只有连接尚未建立且明确不存在/被拒绝才回退，不在响应丢失后重放变更。离线 list 用只读数据库，不建目录、不恢复日志、不初始化 provider；离线 retry 要求队列所有权且不启动网络请求。
- `Journal.Requeue` 只允许 dead 状态，原先可把 uploading/done 重置为 pending 的漏洞已关闭。在线 drop 暂时明确拒绝，离线 drop 不再启动上传器；仍需完成前述 VFS 清理闭环。
- 验证：原断网恢复测试连续 20 次通过；CLI/control/upload/journal/chaos 三轮 race 回归通过。覆盖延迟任务、在途等待、死信、取消后再上传、分页、敏感 session 不外泄、请求校验、拒绝重放、离线只读及所有权隔离。

## 2026-09-05 第二批：阿里上传内容证明

- 增加通用 `provider.UploadContentReader` 上下文契约，由 uploader 为每次 BeginUpload 注入本次不可变 blob 的读取器；不在上层按驱动名分支。
- 阿里优先使用上下文内容，仍兼容直接调用驱动时的 ProofBytes 回调。严格拒绝短读/超长证明；取消错误向上传播，其他读取失败保留普通上传回退。
- 原始证明读取有 64 KiB 分配上限，以及负数、越界、整数溢出、截短和取消检查。115 SHA1 区间证明保持流式读取，不受这个字节回调上限影响。
- 新增 journal → uploader → instrumentation → 真实阿里驱动 → HTTP 回放的完整预哈希命中/内容证明/秒传测试，不直接注入驱动回调。provider/aliyun/upload 三轮 race 回归通过。真实账号仍未验收，保留所有 `UNVERIFIED` 标记。
- 本批最终全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...` 通过；gofmt 检查干净。FUSE 真机测试仍因本机缺少 macFUSE/Fuse-T 跳过，不能视为真机验收完成。

## 2026-09-05 第三批：账号入口状态校准

- 当前 `config add/auth/list` 已实现参数式新增账号、存储方式列表、隐藏输入、stdin 导入、离线迁移和单账号根目录检查。普通账号管理无需建立挂载/VFS/缓存。
- 阿里/百度浏览器授权、115 PKCE 扫码授权已有 HTTP 回放测试；限定回环回调、检查 state/Host/方法/重复参数、限制响应体，单次交换不自动重试或跟随重定向。错误不输出 code/token。
- 重授权检查原配置快照；旧 daemon 不覆盖新授权的引用。仅导入 refresh/access token 时移除另一类旧 token，避免混用账号。检查在凭据保存之后执行，检查失败仍保留已轮换的授权。
- 此处是对已存在代码的核对与文档同步，不代表已完成真实浏览器、手机扫码、终端输入或系统 keyring 验收。相关代码包含在本轮全库 race 验证中。

## 2026-09-05 第四批：固定缓存与在线管理

- VFS 统一 Pin/Unpin，规则先持久化再下载；缓存空间不足返回失败并保留意图和已下载块。等待该文件 write-behind 结束并检查完整性，避免磁盘写入失败后假报成功。
- 缓存增加独立的用户固定状态，不修改待上传数据的临时保护。启动只用本地元数据恢复防淘汰标记；daemon 在 journal 恢复后补齐缺块，目录/增量变化唤醒后台检查，失败通过 status 警告暴露。
- 路径规则、重叠父子规则、同一远端文件的多个路径别名、挂载配置 `pin: true`、改名释放、上传落地后的新缓存身份均纳入处理。新建后台 worker 的启动/关闭互斥，关闭后不能再启动。
- 修复 LinkFile/AdoptFile 未记录缓存身份的问题，使完整文件在重启后仍可被缓存清单和 pin 规则识别。
- `GET /cache/stats|pins`、`POST /cache/pin|unpin|warm|gc` 使用现有 VFS；CLI 同步接入，MCP 增加 unpin。新接口验证本地主机、浏览器来源、变更请求头、JSON 与参数；上传与缓存请求复用单连接、不重放的控制客户端。
- 离线 CLI 在构建 provider/打开缓存前取得存储所有权，关闭后台预取、增量轮询与上传；真实 HTTP WebDAV 回放确认只下载目标内容，已有 pending 上传不被恢复/启动。
- 测试覆盖空间不足、磁盘失败、重启恢复、缺块续取、临时保护隔离、父子规则、多路径别名、改名、新文件发现、上传后保留、并发 pin/unpin/read、worker 启停、MCP 允许列表，以及在线/离线 CLI 与接口拒绝路径。VFS/MCP/control/CLI 三轮 race 回归与全库 race 已通过；最终结果以本轮收尾记录为准。
- **仍未完成**：完整 hydrate/上传复用文件的统一预算与自动 GC；大规模规则恢复/改名的性能基线；真实 FUSE/passthrough 挂载；其余原始目标清单。新文档 `cache-management.md` 明确这些限制，不将模拟通过扩大为真机验收。
- **本轮收尾验证**：包含最终 worker 启停回归的 `go test -race ./... -count=1 -timeout 120s` 全库通过，`go vet ./...`、`go build ./...` 通过，gofmt 无待格式化文件。挂载专项复查确认 FUSE 测试因缺少 macFUSE/Fuse-T 跳过，真实挂载仍未验收。总体目标保持进行中。

## 2026-09-05 第五批：完整缓存计量与日志空间背压

- 核对并补齐完整文件的唯一 inode 计量、年龄/额度淘汰、打开租约、原子替换、hydrate 双副本预留、重启临时文件识别和 GC。已提交 journal 的零分配硬链接允许超额，但如实计量并保护，不能为了缓存额度隐藏已确认的写入。
- daemon 把 journal staging 接到缓存的共享磁盘预留器。写入、覆盖写、truncate 扩容、采用已有 staging 均走准入；检查目标路径所在文件系统，不把缓存目录的剩余量套用于其他卷。并发日志写入、缓存待落盘内容、hydrate 都参与余量检查。该策略保守且不能控制其他进程或文件系统元数据开销。
- 修正 staging 部分写入后失败时不更新长度/哈希的问题，串行化同一 staging 的读写、截断、哈希和关闭；VFS 对部分成功写入保留 dirty 标记。缓存/底层 ENOSPC 传到 FUSE 时不再误报 EIO，MCP 提示检查磁盘空间及相关额度。
- 压力回归发现新计量仍会漏掉“已从索引淘汰但后台仍在写”的副本：64 字节预算曾被观察为 64 字节内容加 16 字节在途副本。已保留退役 flush 额度至临时文件清理结束，并将开始 flush 的状态转换与容量准入互斥；实际磁盘 IO 仍并行。避免重复 worker 为同一块同时生成副本。
- 原“不断替换同一块”测试仅给一个块额度，与在途旧副本也计量的新规则冲突；此数据正确性测试改为给两个 worker 加当前块的明确额度，并新增独立紧预算测试断言拒绝覆盖、拒绝重复使用退役副本额度。不是删除或忽略 ENOSPC 测试。
- 状态、Prometheus 与 CLI 展示完整文件/租约/遗留临时内容以及缓存/日志在途预留；用户文档明确这些字段不能重复求和，也不把 max_size 描述成 journal 或全目录硬配额。
- 验证：缓存整包五轮 race 通过；最终包含 daemon → VFS 写入拒绝、控制面计量测试的全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、`go build ./...` 通过。真实 FUSE/passthrough、系统 keyring、云盘账号和大规模性能仍未验收。
- **仍需处理的正确性问题**：go-fuse backing ID 按 inode 共享，但本项目 passthrough 缓存租约按单句柄释放；后续打开句柄也可能复用旧 backing，必须处理多句柄和读写交叠，不能以普通用户态租约测试替代。原始其他未完成模块保持在工作清单中，总体目标未完成。

## 2026-09-05 第六批：共享 backing 生命周期与默认安全门

- 上轮为实际开发和验证进展。本轮重新检查当前 go-fuse v2.11.0 和内核源码，确认共享 backing 的租约提前释放，以及后续写句柄复用旧 backing，是两个不同问题。
- 新增内核注册回调代理：offer 只有注册成功才转交共享注册表；第一个文件句柄关闭不释放已注册租约；最终注销成功或连接关闭后释放。注销失败保留缓存计量，不提前宣称空间可用。未接回调代理的裸适配器不提供 fd。
- 挂载通过 NewNodeFS/NewServer 接入代理，不修改第三方源码。Init 保留代理，OnUnmount 在连接关闭后清理注册；Mount.Wait/Unmount 等待此清理完成。文档记录对 v2.11.0 初始化行为的依赖，升级必须复核。
- 使用真实 go-fuse 分发层和复制 fd 的注册替身验证首个/最后一个句柄释放、失效后的读取与 GC、注册失败回退、注销失败、连接断开、未注册 offer 和并发循环。共享 backing 专项 20 轮 race 通过；默认保护及相关专项另做五轮回归。
- **仍未解决**：内核同 inode 的 cached/passthrough 模式、不同 backing 及 mmap 的约束意味着逐句柄独立注册、简单加 DIRECT_IO 都不能当成完整修复。读写交叠和新旧版本切换仍需整体实现并做真机验收。
- 为避免已知路径绕过 journal，默认停止自动启用实验 passthrough；显式实验开关保留用于隔离验收，关闭开关优先。默认模式集成测试断言已有读者时写入仍产生 journal 记录、不可变缓存内容不变。**临时开关不是 T-08 的完成，不改变原目标范围。** doctor 不再把硬件可用误报为已协商启用。
- 最终代码通过全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建及 `GOOS=linux GOARCH=amd64 go build ./...`。增加真实内核租约专项，但本机缺 macFUSE/Fuse-T，挂载测试跳过；Linux 交叉编译不是 Linux 内核验收。
- 证据、内核源码链接与后续验收要求见 `docs/fuse-passthrough.md`。Copy、MCP Resources、搜索、其余驱动与分发等原始清单仍未完成，总体目标保持进行中。

## 2026-09-05 第七批：Copy 入口、缓存复用与回归测试

- 上一轮分析产生了可执行证据：生产构建可用，但 Copy 测试的两处返回值接收错误使 VFS 测试无法编译，Journal 的符号链接拒绝测试在 macOS 连续三次失败。本轮修正调用签名，并在 Link 前检查源路径类型/身份，保留 Link 后的 inode 和大小检查；未通过放宽测试规避平台差异。
- 当前 Copy 使用服务端能力分支；不支持时复用完整缓存的不可变硬链接或有界流式准备，再进入正常 journal 上传路径。拒绝格式不完整的服务端成功结果和缓存长度不一致，结果不确定时不回退重放。链接 staging 禁止修改，并处理相同 inode 的 rename 不消费原别名这一情况。
- CLI cp、控制面 POST /copy 与 MCP copy 已接线。在线 CLI 不重新构建 Provider；离线必须取得所有权且不启动后台上传。MCP 同时检查源与目标允许列表、只读模式；控制面检查本地主机、来源、专用头、JSON 和路径。没有把复制请求超时误当成可安全离线重试。
- 新增测试覆盖：普通复制、空文件与 strict 等待、打开写者的已提交快照、本地待传版本复用、16 个并发创建者唯一胜者、取消/空间不足不提前发布、服务端能力与错误不重放、无效远端结果、缓存长度不符、跨 remote 缓存 inode 复用、journal 重开后的秒传零 UploadPart、在线/离线 CLI、所有权隔离、真实 MCP 传输及 HTTP/Unix 控制接口。
- 验证：全库 `go test -race ./... -count=1 -timeout 120s` 通过，`go vet ./...`、本机构建、Linux amd64 交叉构建通过；Copy/链接 staging 专项 10 轮 race 通过，gofmt 检查无输出。真实 FUSE 测试明确因 macFUSE/Fuse-T 缺失跳过；非测试 UNVERIFIED 仍为 51，未做真实账号验收。
- **未完成且不缩减范围**：下载准备阶段持久化 Copy 作业、服务端结果对账、跨端同名创建的原子无覆盖契约、发布故障窗口。已有 journal 上传恢复不能证明任意时刻 kill -9 都能自动恢复整个 Copy。`docs/copy.md` 明确这些边界；TODO/README/MCP 工具说明已同步，T-04 仍为部分完成。MCP Resources、搜索、其余驱动、服务分发、产品入口等原始目标保持开放。

## 2026-09-05 第八批：日志到本地版本的发布恢复

- 上轮为实际开发进展。本轮发现日志提交后、元数据发布前上传器就能领取任务；Copy 的 Release 失败后不再保留可重试句柄，启动也只恢复队列，无法补齐目标本地版本。该窗口同时影响普通写入。
- Journal schema v4 增加 `needs_publish`：新 VFS 提交先持久化发布意图，Claim 排除待发布行；缓存链接安装在元数据更新之前，发布完成后才放行上传。不改变普通直接入队的默认行为，重试/会话状态不因解除标记而重置。新版本只读查看旧 v3 库无需迁移，写打开才升级。
- `RecoverPublications` 在 journal 验证 payload 后、daemon 启动上传/刷新前完成中断发布，保留当前路径和远端版本，拒绝把旧提交覆盖到新提交上。已确认待传版本的缓存链接丢失时从 journal 重建。缺失 inode 无法区分删除和元数据损坏，因此保留死信和 payload，既不重建目标，也不删除唯一的持久化数据。
- 元数据库默认 NORMAL，不能仅用其普通事务返回作为 power 模式的发布门槛。新增连接隔离的 FULL 发布事务，提交后恢复原连接同步设置；目录缓存更新仍用原有策略，crash 模式不额外要求 FULL。真实掉电与新增同步开销的性能验收仍待补，未用子进程强杀冒充断电验证。
- CLI/控制面上传列表展示待发布状态。持续停留在该状态时需检查缓存/元数据错误后受控重启恢复，不开放绕过标记的强制上传。
- 测试包括：未发布行不可 Claim、重复放行、v3 只读与迁移、FULL 同步及连接恢复、缓存发布失败不暴露新身份、离线读回、已删除 inode 的替代文件不受影响且旧内容保留死信、已确认写入丢失元数据后的内容保留、缓存链接重建、daemon 重开后立即可读并继续上传。真实子进程在日志提交后的故障点被强杀，父进程重建所有存储组件、离线读回并完成上传；未复用子进程内存。
- 最终验证：全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建、Linux amd64 交叉构建通过。发布/迁移/FULL 事务专项五轮通过，daemon 启动发布专项单独五轮通过；最终缺失 inode 增量也纳入全库 race。gofmt 无输出；真实 FUSE 专项仍因缺少 macFUSE/Fuse-T 跳过。
- **原始目标保持开放**：Copy 下载阶段持久化作业、日志提交前占位节点、服务端结果对账与并发无覆盖契约仍未完成；本轮只证明日志提交后的本地发布恢复。MCP Resources、搜索、其余驱动、服务分发、产品入口、真实账号/内核/性能等剩余工作未缩减，整体未完成。

## 2026-09-05 第九批：复制准备作业与检查点存储

- 上轮为实际开发进展。本轮新增 Journal schema v5 的 `copy_jobs`，记录源对象/版本/长度和目标身份。准备 payload 独立保存在私有 copies 目录，不能与普通未提交 staging 一起被启动恢复清掉。
- `BeginCopy/Write/Checkpoint/ResumeCopy` 实现顺序追加、大小与空间准入、文件及目录同步后才推进偏移、恢复前缀 CRC32C 校验和未确认尾部截断。恢复时一次重建 SHA/MD5/前缀哈希流，避免后续每个检查点重新扫描全文件。损坏/截断/符号链接拒绝续写并保留内容。
- 准备作业对同一 remote/父对象/文件名持有独占预约，同一作业只能有一个准备句柄，非 owner 和只读 journal 不得写入。Close 只关闭句柄，不是取消；FailCopy 保留数据且不能撤销已经提交的上传。
- `Submit` 原子地新增固定 ID 上传行并记录 submitted，禁止覆盖已有上传 ID，数据库失败同时回滚两边；可以携带已有 needs_publish 门禁。提交后 payload 所有权转给上传行，重复提交或重新当成下载恢复被拒绝。
- `BeginCopyFromWhole` 保留零分配硬链接路径，链接/校验/同步完成后直接保存 ready 作业，不让共享 inode 经历可截断的 preparing 状态。恢复后的 ready staging 仍不可修改；释放私有链接不改变源文件。
- 回收先解析全部所有权，保留准备/完整/失败作业及上传行引用；删除上传也检查准备作业所有权。payload 存绝对路径，覆盖同一个 journal 从相对路径配置到绝对路径重开的恢复场景。
- 测试覆盖完整/部分/空文件、恢复后所有哈希、SQL 与 sync 失败不推进、原子交接回滚、并发目标与句柄互斥、只读/空间拒绝、所有权与孤儿清理、源 inode 不变及相对路径重开。子进程在已确认前缀后继续写未确认尾部再被强杀，父进程重开 journal 后续写为正确内容并交接唯一上传行。此测试属于准备存储层，不是 cp 端到端验收。
- 最终全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建和 Linux amd64 交叉构建通过，Copy/迁移专项五轮 race 通过，gofmt 检查无输出。真实账号/内核/物理掉电及大规模性能仍未验收。
- **尚未接线，整体未完成**：当前 VFS Copy/CLI 仍使用原准备路径，新作业尚不提供自动下载续跑。下一步必须接入源版本与挂载校验、目标 inode 的持久化对应及删除/改名竞争处理、daemon 调度与作业管理入口，然后从真实 cp 中途强杀做完整验收。服务端对账和其他原始模块继续保持开放；具体约束见 `docs/copy-preparation.md`。

## 2026-09-05 第十批：准备恢复接线验收与引用缓存修复

- 本轮核对当前工作树确认 VFS Copy 已接入 `BeginCopy/BeginCopyFromWhole`、1 MiB 检查点、源版本/挂载身份检查、目标绑定及后台恢复；这部分代码已先于旧文档存在，不能继续描述为未接线。
- 元数据库 schema v6 持久化独立身份，完整目标节点与复制 ID 在同一 FULL 事务中绑定，删除后仍保留绑定。写打开与 namespace 变更和复制交接串行化，准备目标写打开/改名返回忙，删除取消准备任务。daemon 的后台栈启动/停止复制调度，离线 `NoBackground` 栈不续跑。
- 新增真实子进程强杀测试：VFS Copy 下载至 1 MiB、完整目标绑定后但上传交接前两个故障点被父进程强杀，重开全部存储，由 `StartCopies` 自动续跑。断言仅下载检查点以后的字节、完整读回一致、绑定 inode 不变、唯一上传行且发布门禁已解除，最后完成上传。
- 新增源对象同 ID/长度但版本变化、挂载根变化、只读、元数据库重建和取消等拒绝场景；失败保留检查点，源变化不下载字节、不发布目标。删除绑定后后台扫描不复活目标。daemon 重开测试断言离线不交接、正常后台恢复并完成上传。
- 回归复现并修复：独立改名的持久化节点导致 BindCopy 拒绝时，错误清理无条件 Forget 该复制的缓存，使仍引用本地版本的文件读不到内容。现在查所有元数据别名，仅在确认无引用时清理；查询错误保守保留。该测试修复前稳定失败为 local-only cache missing，修复后通过。
- Copy 恢复/绑定/daemon 专项五轮 race 及全库 race 已通过；已同步 Copy 文档与 TODO 的过时接线描述。真实 CLI 子进程（非 VFS 测试入口）的下载中途强杀仍待独立验收，现有测试不能替代真实账号、内核挂载或物理掉电。
- **整体目标仍未完成**：复制作业管理/回收、准备目标改名、服务端未知结果对账、跨端原子不覆盖、在线上传丢弃清理、MCP Resources、搜索、其余驱动、服务分发、UI/WebDAV/媒体入口及真实环境验证继续保持开放。

## 2026-09-05 第十一批：生产 CLI 强杀验收与复制作业查询

- 上轮为实际测试和修复进展。本轮新增生产二进制端到端测试：现场 `go build` 后运行真正的 `cloudfs cp`，源与目标为两个 HTTP WebDAV 模拟服务。在第二段 Range 请求阻塞时强杀复制进程，检查持久化准备状态为 preparing、偏移为 1 MiB，再启动独立生产 `cloudfs mcp --stdio` 进程接手续跑。没有使用测试专用 provider、VFS 故障钩子或子进程内存。
- 验收确认新 daemon 仅下载检查点之后的剩余字节，目标收到一次完整 PUT，复制交接后上传队列排空。该测试验证真实 CLI 与 daemon 进程链路，不代替真实云盘、FUSE 或物理掉电验收。
- 新增只读 `copies list/show` 与 `GET /copies`。Journal 按稳定 UUID 游标分页，默认 200、最大 1000；可查询 failed/preparing/ready/submitted，提交后返回关联 upload ID。submitted 不表示上传成功，不伪造终态历史。
- CLI 优先查询已运行的控制端点；仅明确未连接时离线只读打开 journal，不初始化 provider/meta/cache、不恢复任务、不接管上传，也不截断活动准备的未确认尾部。无准备表的旧版只读日志返回空列表且不迁移。缺失日志的 list 不创建目录，show 返回不存在。
- 输出省略内部对象 ID/版本、数据库身份、payload 路径、hash 及原始持久化错误；失败只给内容保留的通用警告。HTTP 检查本地主机与浏览器来源，拒绝错误方法、未知/重复/空查询参数、不规范 ID/cursor 及越界 limit；数据库错误不向 HTTP 暴露内部路径。
- 新增分页无重复/漏项、旧库不迁移、在线/离线活动 owner 隔离、未确认尾部保留、敏感字段不外泄、HTTP 参数/来源拒绝、空列表不建目录，以及 submitted 不误报成功的回归。专项 race、全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建及 Linux amd64 交叉构建通过。
- **仍未完成**：MCP 复制作业查询、各入口重试/取消与终态回收、准备目标改名、服务端对账及跨端原子不覆盖。真实账号/内核/掉电/长期性能和其余模块仍保持在整体目标中，未因本轮验收而缩减。

## 2026-09-05 第十二批：准备任务重试/取消与保留版本恢复

- 新增 `copies retry/cancel <id>` 及 `POST /copies/retry|cancel`。取消持久化 cancelled 并中断当前准备下载；Checkpoint/Submit 的数据库状态条件阻止后续交接。已 submitted 明确返回冲突，不能把准备取消误报为停止远端上传。取消不删除 payload，已发布完整本地版本继续可读。
- Journal schema v6 增加 cancelled 和 revision。重试仅接受 failed/cancelled，验证 CRC 前缀、恢复哈希状态并截去未确认尾部；完整共享 inode 保持不可变。重试以旧 revision 条件更新，每次取消都递增 revision，即使取消发生在重试校验期间或作业本就 cancelled，旧重试也不能覆盖新取消。目标预约唯一约束仍生效。
- v5 表及旧迁移中途留下的准备表在一个事务中重建，保留原意图。发现 Journal.Open 原来即使未取得 owner 也会迁移，已改为非 owner 不执行 DDL，旧版本库需由 owner 升级；只读查看 v3/v4/v5 仍不迁移。
- VFS 显式重试预检查元数据库/挂载绑定和原目标是否删除、改名或被占用，恢复/发布继续校验。排队成功唤醒后台 worker，不等待一分钟。离线取消不初始化 provider；离线重试构建 VFS 并做启动恢复，但不启动后台传输。CLI 请求连接后的错误不触发离线重放。
- 回归复现并修复：准备任务已有完整本地目标但尚未交接，取消后重启若缓存链接丢失，原恢复只看 uploads，目标会变得不可读。新增完整保留内容的校验/租约入口；启动只为当前元数据库仍引用的 ready/failed/cancelled 版本重建缓存链接与临时 pin，不创建缺失节点、不重新下载、不交接被取消任务。测试修复前失败为 local-only cache missing，修复后通过。
- MCP-only 模式作为 journal owner 时现在也启动原生控制端点；否则 CLI 无法在线管理它的准备任务。顺带修复 `mcp install` 在已有有效配置时先启动 daemon/上传的问题，注册操作提前返回且不创建存储。生产二进制测试覆盖无效 provider 的注册输出，以及真实 MCP-only owner 的查询和状态拒绝端点。
- 新增取消/交接竞争、较晚取消不被重试覆盖、前缀损坏、目标预约冲突、旧库/半迁移/非 owner 升级拒绝、活跃下载中断、检查点续跑、worker 唤醒、目标删除拒绝、取消保留版本重建、CLI 在线/离线隔离、HTTP 来源/方法/参数拒绝及响应丢失不重放测试。
- 最终包含 MCP-only 控制接线的全库 `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建及 Linux amd64 交叉构建通过；取消/重试/管理专项三轮 race 通过，gofmt 全库检查无输出。FUSE 真机、实际云盘与物理掉电未在本轮验证，仍不计作验收完成。
- **未缩减的剩余范围**：MCP 作业管理工具、已交接上传取消与在线 drop、历史及 payload 清理、准备目标改名、服务端结果对账及跨端原子不覆盖尚未完成。其余驱动、MCP Resources、搜索、服务分发、UI/WebDAV/媒体入口、真实账号/内核/掉电/长期性能仍在原目标中。

## 2026-09-05 第十三批：显式复制清理验收与恢复测试时序修正

- 当前工作树已接入 `copies forget <id> --confirm`、`POST /copies/forget` 和 VFS/Journal
  两阶段清理。本轮补齐接口及故障回归并校准文档，不能继续描述为没有清理命令。
  清理不删除本地/远端文件，不取消上传；仍有本地元数据或任意上传引用时拒绝。
- Journal schema v7 的 `copy_cleanup` 保存不可重试为下载的 purging 意图。删除缓存、
  私有 payload 或最终数据库提交失败时仍可查询并继续清理，正常 daemon 会恢复此前
  已确认的清理。新上传不能引用正在清理或已清理的准备 payload。
- 元数据库 schema v7 在确认无本地引用后执行真实 FULL 写事务，同步此前的本地删除，
  才允许销毁持久化内容。旧复制绑定墓碑保留，防止旧日志复活已删除目标。独立 cache
  `ForgetChecked` 报告 unlink/fsync 失败；普通缓存失效不额外增加目录 fsync。
- 新增 CLI 在线/离线忘记记录、缺确认不改状态/不建目录、活动状态拒绝；HTTP 增加
  本地引用冲突、成功后查询/重复清理 404、purging 可查询和重试完成、来源/方法/参数
  拒绝，以及清理请求响应丢失不重放。在线命令沿用原 daemon，不构造无效的配置 provider。
- 新增块缓存/完整缓存删除失败保留计量并重试、符号链接只删除名称、VFS 缓存失败不
  删除 journal payload。已有 Journal SQL 回滚/重开、引用和活动句柄拒绝、源硬链接
  保护，以及 VFS 清理中强杀恢复/打开租约保护测试一并纳入全库验收。
- 上轮分析实际发现全库测试在 `TestCopyPreparationResumesAfterProcessKill/bind` 偶发
  失败：测试看到 submitted 就停止 worker，取消发生在 Submit 与 MarkPublished 之间，
  因而看到 `needs_publish=true`。改为等待交接和发布两个条件，再停止 worker；新增
  确定性测试主动在此窗口取消，断言原上传被门禁阻止、恢复两次不重复交接、不重新下载，
  内容完整并可继续上传。没有将 submitted 改成远端完成，也没有删除原发布断言。
- 验证：全库 `go test -race ./... -count=1 -timeout 120s` 通过，`go vet ./...`、
  本机构建及 Linux amd64 交叉构建通过；恢复/清理/缓存/CLI/HTTP 专项五轮 race
  通过，gofmt 检查无输出。当前 macOS 无 macFUSE/Fuse-T，挂载相关测试仍 SKIP；真实账号、
  物理掉电和长期故障不计作本轮完成。
- **整体仍未完成**：清理仅补齐无引用准备内容的显式回收，不替代完整终态历史、
  已交接上传取消和在线 drop。MCP 作业管理、准备目标改名、服务端对账、跨端原子
  不覆盖、其余驱动、Resources、搜索、服务分发、UI/WebDAV/媒体及真实环境验收继续开放。

## 2026-09-05 第十四批：MCP 资源列举、受限读取与客户端缓存隔离

- 上一轮已完成复制清理验收。本轮转入原清单中的 MCP Resources，新增
  `internal/mcpsrv/resources.go`，注册 `resources/list`、模板与 `resources/read`；
  尚未实现订阅，不设置 subscribe 能力，T-03 保持部分完成。
- URI 使用 remote authority 加完整虚拟路径，校验最深挂载归属；同一 remote 的多个
  挂载不会混淆。非 URI-host 字符的 remote 名称采用无碰撞编码。资源入口按配置挂载
  与允许列表取交集，列举不访问 provider；嵌套挂载使用其实际 remote 的 URI。
- 文件资源经 VFS 返回有界文本或 blob，空内容在协议上显式表示为空 blob；字节范围
  有上限，UTF-8 截断页保持字节精确，EOF/极大 offset 不溢出。文件续页使用内容
  `_meta` 中的 next URI。目录 JSON 同时按项目数及编码后字节数分页，子项 URI 正确
  转义；无法容纳单项时明确报错，不循环生成空游标。
- 路径穿越、无效编码、挂载冒用、未知/重复参数在访问 provider 前拒绝；底层错误
  不向资源调用者透露签名链接、凭据或缓存路径。资源读取不改变只读工具约束，HTTP
  沿用现有 bearer 认证。读内容不是跨请求版本快照，目录扫描也仍有完整列表开销。
- 通过失败测试确认 SDK v1.7.0 默认把资源响应的 cacheScope 设置为 public。新增
  SDK 处理后的资源专用中间层，将内容、根列表和模板统一设 private、TTL 0，不修改
  依赖包，也不影响原工具结果。回归通过同一客户端写后再次读取，检查内容与缓存字段。
- 新增真实 SDK 内存传输与 Streamable HTTP 回归：资源/模板分页、允许列表、挂载
  别名和遮蔽、非 ASCII 名称、文本/二进制/空文件、UTF-8 分页、目录字节预算无丢项、
  非法 URI 零 provider 调用、只读与 HTTP 认证、私有缓存元数据和写后可见性。
- 验证：Resources 专项三轮 race、全库 `go test -race ./... -count=1 -timeout 120s`、
  `go vet ./...`、本机构建和 Linux amd64 交叉构建通过。真实账号、内核挂载与物理掉电
  不计作验收；当前机器缺少 macFUSE/Fuse-T，内核测试仍跳过。
- **后续仍在范围内**：Resources 本地写入/远端 delta/TTL/删除/目录改名的事件流和订阅
  生命周期、过载与去重语义，以及大目录分页内存优化。不能直接用会跳过内核来源的
  FUSE 失效回调充当变更流。Copy 剩余管理/对账、在线上传 drop、搜索、其余驱动、
  服务分发、Web/UI/媒体及真实环境验收均未缩减。

## 2026-09-05 第十五批：独立 VFS 变更流

- 上轮完成资源读取入口。本轮新增 `FS.WatchChanges`，并接入本地文件创建/提交、
  建目录、改名/删除、复制完整版本发布、上传落地/特殊刷新、丢失版本修复及远端
  delta/目录 TTL 刷新。它独立于 FUSE 失效回调，因此不会漏掉原先刻意跳过内核
  自失效的写入；也不改变原 FUSE 行为。
- 变更提示只包含虚拟路径与子树/重查标记。改名在同一提示中保存新旧路径，删除
  后保留已消失名称。未变化的目录刷新与重复 delta 不产生提示；复制绑定完整本地
  版本后、上传交接前即通知，后续交接失败不能让已经可读的内容保持静默。
- 每消费者队列固定 64 条，过载合并为 Rescan，批量路径超过 128 个则退化为目录
  子树提示/重查。发送无消费者回调与每事件 goroutine，不等待慢读者；路径切片
  独立，取消幂等，并发发送/取消/关闭由锁保护，FS 关闭后不接受新订阅。
- 新增内核写入且无内核自失效、改名/删除后代匹配、delta 更新/删除、未变化刷新、
  列表新增/删除、2000 次发送下的慢/快消费者隔离、切片隔离、并发取消/关闭，及
  复制交接失败但事件内容已经可读的回归。目录删除测试显式推进假时钟，避免把
  同一时刻新建节点的既有防误删保护误判成通知丢失。
- 变更流专项五轮 race、包含生产接线的全库 race、vet、本机构建和 Linux amd64
  交叉构建通过，gofmt 检查无输出。最新增加的取消/复制故障测试也已纳入专项五轮。
- **未缩减剩余范围**：MCP 订阅注册/取消/断连、通知合并、权限过滤与新版订阅流
  仍未接入，不声明 subscribe 能力。本流是可合并的重新读取提示，不是持久审计
  日志，不能用它代替协议层去重与生命周期验收。原来的 Copy/上传管理、搜索、
  其余驱动、服务分发、UI/WebDAV/媒体和真实账号/内核/长期性能验收继续开放。

## 2026-09-05 第十六批：MCP 订阅协议、HTTP 兼容与关闭修复

- 资源订阅接入独立 VFS 流：注册前验证 URI/挂载/允许列表，批次重复或部分越界整体
  拒绝，不进行 provider IO；缺失路径可订阅等待创建。固定 25ms 合并窗口，按原 URI
  通知目录/文件/分页资源。注册数、SDK 会话数及新版 listen 流均有上限。
- 新版监听确认后才激活发送。确认前发生的变更保留；同 URI 多个会话各用自己的
  subscriptionId，SDK 广播不能把待确认/已取消的关系当成有效收件人。旧版重复订阅
  幂等，同会话同 URI 的新版重叠监听明确拒绝，避免 SDK 单 ID 映射被另一流取消。
- 新增真实 HTTP 测试首先复现默认 stateful handler 令 SDK 降级旧协议、没有新版
  acknowledged 的问题。生产端点现按协议选择 stateful/stateless，保留旧版
  initialize/session/GET；新版取消 POST 响应后立即释放，不依赖下一次通知写入。
  旧版会话增加 5 分钟 POST 空闲过期，客户端需 keepalive。
- 实测 SDK v1.7.0 客户端取消后额外发送无新版元数据的旧式 notifications/cancelled，
  HTTP 400 会把连接置失败，重新订阅/读取失败。增加经过认证和跨源检查的窄兼容层，
  仅对格式有效且最多 64 KiB 的取消通知回 202；不查 requestId，不取消其他流。
  回归证明伪造另一个活动流的 ID 不会取消它，真正的原流断开仍释放资源。
- 关闭测试复现只订阅目录变化的 auto-listen 未被管理、Server.Close 永久等待。
  现跟踪并取消所有新版 listen（不为纯目录订阅建立 VFS 消费者），stdio/HTTP 与
  服务关闭共用生命周期管理。
- 安全回归复现 isLoopback 将空 host 判为本地，令 `:端口` 可无 token 绑定所有网卡；
  修复该判断。加入 HTTP 跨源保护，并对旧版 GET 流及 HEAD/OPTIONS 的 Origin 校验。
  listener 失败时取消 shutdown 等待 context，避免留下永久等待的 goroutine。
- 新增/扩展本地与远端变更、多会话隔离、确认屏障、非法批次、资源/会话数量上限、
  改名/删除后代、尚不存在目录、真实新旧 HTTP 通知/取消/重新订阅、目录订阅关闭、
  wildcard 认证、冗余取消及跨源拒绝测试。资源读取 HTTP 测试改用生产路由，并为
  每个权限拒绝使用独立连接：SDK 的其他 HTTP 4xx 仍可能关闭客户端连接，不能用
  “连接已关闭”的错误假充另一次权限拒绝。未修改协议错误状态来掩盖客户端限制。
- 验证：mcpsrv 整包三轮 race；最终订阅/HTTP 安全专项五轮 race；全库
  `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建、Linux amd64
  交叉构建通过；gofmt 无待格式化文件。当前仍没有 macFUSE/Fuse-T，不计真实内核验收。
- **保持开放的范围**：慢连接的通知延迟隔离、SDK 旧版断连后空 URI 容器的长期回收、
  目录分页扫描内存和真实环境验收仍缺；T-03 保持部分完成。Copy 对账/管理、在线上传
  drop/本地版本处理、搜索、其余驱动、服务分发、Web UI/WebDAV/媒体、真实账号/内核/
  掉电/长期性能和许可证决策等原始目标未缩减，整体目标未完成。

## 2026-09-05 第十七批：MCP 复制准备管理与源路径恢复校验

- 新增 `list_copy_jobs/get_copy_job/retry_copy_job/cancel_copy_job/forget_copy_job`，
  MCP 工具数由 17 增为 22。查询通过 VFS 的安全字段投影，修改复用原 VFS 接口，
  不另造上传/日志管理链路。取消保留内容，重试返回排队接受，forget 必须显式确认，
  已交接上传/仍被引用的版本明确拒绝，不把 submitted 或 accepted 当作远端完成。
- 任务必须同时满足源/目标允许列表、当前元数据库身份以及当前实际挂载的 remote、
  前缀、根对象；旧数据库/重挂账号/嵌套挂载遮蔽的任务不暴露给受限 MCP 客户端。
  路径缺失本身不影响历史查询，查询不恢复任务、不下载、不访问 provider。
  未授权 UUID 和不存在的 UUID 返回相同错误；内部 ID、版本、哈希、payload 和原始
  错误均不在公开投影内。Server.New 复制 Allow 切片，外部修改不能扩大已建服务权限。
- 每次列表只处理 200 条日志任务（另读一条判断续页），再进行当前挂载与路径过滤，
  响应按 MaxEntries/MaxBytes 截断且不跳过未返回的可见任务。隐藏任务可产生空续页，
  需继续游标。游标使用实例级随机 AES-GCM 密钥认证加密，不泄露隐藏任务 ID、不可
  伪造或跨实例复用；重启后重新扫描。分页不是快照，不返回全局任务总数。
- MCP 活动复制测试在下载第二段阻塞时列出任务、通过管理工具取消，验证复制调用
  返回失败、1 MiB 检查点保留；通过工具重试后由 VFS 恢复，目标逐字节一致并唯一
  交接为 submitted。测试夹具无后台 daemon，显式调用其共用 ResumeCopy 入口，不
  把这种测试写成生产后台调度验收；既有生产 cp 强杀/独立 daemon 验收继续回归。
- 安全核对发现恢复下载只匹配挂载并 Stat 原对象 ID，源改名后同 ID/版本仍会被追踪。
  新增强杀准备后源改名的失败回归，修复前 ResumeCopy 成功并下载，修复后返回
  ErrCopySourceChanged、不读移动后的内容且保留检查点。恢复新读现在按记录路径选择
  实际挂载并核对源节点身份、版本、长度，之后仍执行原远端版本校验；临时路径查询
  错误保留其重试分类。已 ready 的准备不需要源，新增源路径/远端对象均不存在仍可
  交接的回归。该校验遵循 VFS 缓存一致性，不承诺跨客户端原子权限或内容快照。
- 新增 12 个测试覆盖工具与 VFS 查询、双路径/挂载/数据库权限、只读模式、引用保留、
  加密游标、隐藏空页、响应预算、错误脱敏、Allow 所有权、真实 HTTP 认证与管理、
  活动复制取消/续跑和源路径边界。已有工具注册测试同步验证 22 个工具。
- 验证：复制管理/查询/源路径专项五轮 race、全库
  `go test -race ./... -count=1 -timeout 120s`、`go vet ./...`、本机构建和 Linux amd64
  交叉构建通过，gofmt 无待格式化文件。真实账号、物理掉电和内核挂载不算本轮通过项。
- **整体仍未完成**：已交接上传取消/在线 drop 与本地版本清理、Copy 远端对账、原子
  无覆盖、准备目标改名和完整终态历史仍缺；MCP 订阅慢连接/长期回收、搜索、其余驱动、
  运行分发、Web UI/WebDAV/媒体、真实账号/内核/掉电/长期性能和许可证决策继续开放。

## 2026-09-05 第十八批：恢复搜索、短字符串索引与路径查询

- 分析阶段实测当前工作树缺少 `meta.Store.Search`，元数据测试、MCP 和 CLI 因此
  编译失败。本批补齐 `Search` 与 `SearchWithin`，查询在同一 SQLite 语句快照内
  取得名称和父链路径，不再逐条另查路径造成改名期间的名称／路径混合。
- 元数据库 schema v8 新增 1／2 Unicode 字符的十六进制词元 FTS 表。插入、改名、
  删除触发器在原事务内维护短索引及 pending；复制绑定的直接插入也经过触发器。
  长名称继续后台 trigram 索引。升级回填现有名称，并把镜像名称缺失／不一致的
  节点重新排队；旧数据库已有错误索引不能被升级悄悄继承。
- 同步改名完成后清除旧 pending，后台合并使用 nodes 的当前名称，修复旧排队项
  覆盖新名称的窗口。字面 `%`、`_`、引号不再按 LIKE 通配符或 FTS 语法解释。
- 带斜线的查询按路径字面子串匹配：从最长非空组件的名称索引找候选，重建路径并
  展开后代、验证完整片段；重叠子树去重，递归深度上限 512。目录改名无需重写
  所有后代索引。无斜线保持名称匹配；不自动遍历 provider，只搜索已知元数据。
- MCP 在数据库中先取请求路径与允许列表的交集，再排序限量，修复 `limit*4`
  超采样仍被隐藏结果占满、漏掉可见文件的问题。普通名称搜索用额外一项判断截断。
  内容查询仍受候选数和缓存前缀字节预算限制，耗尽候选预算明确标记 truncated；
  文档删除“任何时候都快／等于完整 grep”的不准确承诺。
- 新增七个测试覆盖 Unicode／标点、pending 改名、目录移动／删除、范围边界、
  v7 升级修复、触发器事务回滚、非法输入、并发改名快照、十万节点查询计划，以及
  MCP 协议层权限过滤后限量。MCP 复制管理测试还暴露夹具未关闭缓存后台写入、
  临时目录删除竞争；增加 Cache.Close 清理，等待写入结束后才删除测试目录。
- 十万节点普通构建实测：带同步短索引插入约 716 ms，`99` 查询约 16 ms，父目录
  改名约 23 ms。查询计划为 FTS postings + inode 主键查找，没有 nodes 全表扫描，
  排序仍用临时 B-tree。这是当前机器并行运行测试时的单次数据，不是生产性能承诺。
- 验证：全库 `go test -race ./... -count=1 -timeout 180s` 通过（含十万节点测试，
  元数据包约 119 秒）；名称／路径／迁移／权限／复制清理专项五轮 race 通过。
  十万节点普通构建独立复跑通过，vet、本机构建、Linux amd64 构建及 gofmt 检查
  通过。重复专项不再五次包含十万节点性能夹具；该重复运行主动中止，未算通过。
  内核挂载专项实际返回 macFUSE/Fuse-T 不可用的 SKIP，不计验收成功。
- **整体目标仍开放**：搜索的海量 pending 长查询、宽路径展开／排序、真实规模长期
  性能仍待优化或验收，T-09 保持部分完成。原 Copy／上传管理与对账、MCP 慢连接／
  回收、大缓存与内核生命周期、新驱动、服务分发、UI/WebDAV/媒体、真实账号／
  系统 keyring／内核／掉电／长期性能和许可证决策不因本批完成而移出范围。

## 2026-09-05 第十九批：上传删除竞争与 staging 对象交接保护

- 继续检查在线 drop 的前置安全条件，发现现有 `DropSuperseded` 在查询旧版本 ID
  后直接无条件 Drop。确定性测试在快照后领取旧任务，修复前返回“删除 1 条”且
  丢掉活动行；修复后事务内重查 pending 状态，保留活动行及内容给 worker 处理。
  同时不再吞掉真正的删除数据库错误。
- `DropPending` 原来先条件删 pending，失败后另查 state，再对 dead/done 无条件
  Drop；管理员可以在后一次检查后重试并领取。现在同一写事务完成状态检查及
  uploads／parts／dead-letter 删除，uploading 返回 ErrInFlight，未知状态拒绝。
  新增重试／领取竞争及附属记录删除失败回滚的回归。
- 第二个确定性失败回归：同内容的新 staging 已改名到 objects、还没有上传行，
  删除旧任务只检查数据库引用，误删了新写入需要的对象。新增对象锁和按 staging ID
  的进程内引用。`CommitStaging` 的 rename 与引用登记对 GC 原子；VFS 在上传记录中
  携带不持久化、不输出 JSON 的 StagingID，Commit 成功才释放这一份保护，失败
  保留以供重试。多个相同内容写者独立计数，重复 Commit 不减少其他写者的保护。
- GC 同时检查临时引用与数据库引用；引用检查至 unlink 期间持有写事务，防止插入
  新行的竞争。文件删除仅允许 journal 私有 objects／规范 copies 名称，数据库行
  不能授权删除任意外部路径。普通对象删除仍是尽力回收，失败可能等重启清理。
  未提交 staging 的临时保护不是掉电承诺，退出后仍可按未确认孤儿处理。
- 新增 6 个 Journal 测试及 1 个 VFS 测试，覆盖快照后领取、死信重试竞争、相同内容
  三个写者与重复提交、提交失败后重试、事务回滚、外部路径保护，以及生产 WriteFile
  成功后删除确实释放临时保护。各测试夹具同步传递 StagingID，既有共享 blob／复制
  准备所有权／恢复／发布／上传回归继续执行。
- 验证：Journal 删除／supersede／失败提交专项五轮 race、生产 VFS 交接专项五轮
  race、相关六包 race 及最终全库 `go test -race ./... -count=1 -timeout 180s`
  通过。vet、本机构建、Linux amd64 构建及 gofmt 检查通过。真实账号、内核挂载、
  物理掉电与长期运行未作为本批通过项。
- **尚未完成的原范围**：本批没有开放在线 `uploads drop`，也没有把 Journal 的
  底层安全修复当作完整管理入口。活动上传停止、VFS 本地版本／打开句柄处理与持久
  清理恢复仍需完成；Copy 对账／原子不覆盖／终态历史、搜索规模优化、MCP 长期负载、
  新驱动、服务分发、Web UI/WebDAV/媒体及真实环境验收等继续保留在整体目标中。

## 2026-09-05 第二十批：持久上传取消与本地版本保留

- 新增 `uploads cancel <id>` 和 `POST /uploads/cancel`，在线请求由原 daemon 的
  Uploader 协调；离线只取得日志 owner，不初始化 provider、不启动恢复传输。
  返回 cancelling／cancelled 及固定保留／远端不确定性说明，不把响应接受当成
  远端回滚，也不在连接后的失败／响应丢失时切到离线重放。
- Journal schema v8 增加持久取消状态语义。pending/dead 可直接 cancelled，活动
  行先 cancelling；worker 退出或启动恢复后才确认 stopped。完成行和删除补偿行
  拒绝取消。旧 Retry/Defer/Succeed/Fail/重复 Commit/Drop/Retarget/Tombstone
  在同一事务内检查停止状态，不覆盖取消或销毁载荷。普通 Requeue 拒绝取消记录。
- Uploader 登记单任务 context 后才做 provider IO，取消与登记共锁，关闭 claim→
  start 窗口。取消与最终本地成功发布串行仲裁：发布先完成则取消冲突；取消先完成
  则即使 provider 忽略 context 并返回成功，也保留取消意图、不发布 done。
  没有活动登记时可确认停止，之后的旧 claim 不发出 IO。cancelling 仍阻挡同 inode
  新版本，直到旧传输退出；普通 worker context 取消仍释放回 pending，不误作管理取消。
- 本地完整版本和上传 session/parts 留存，重启不重新上传取消任务。启动恢复也为
  当前节点仍引用的取消版本重建缓存链接；缺失／已被新版本替代的目标不复活。
  普通删除／改名拒绝保留取消版本，FUSE 映射 EBUSY，MCP 给出明确保留提示。
  flush 不把取消任务算作成功，列表、status、指标与 doctor 展示取消／停止中状态；
  doctor 不自动重试取消记录。
- 新增 Journal 状态转换／重开恢复／非 owner／已完成和补偿拒绝／同 inode 门禁，
  Uploader 活动中断／领取到登记窗口／远端迟到成功／发布先完成／普通取消 context
  释放，以及 VFS 缓存恢复／保留版本约束、CLI 在线离线取消、控制端点状态与安全、
  FUSE errno 回归。扩展无效参数、浏览器拒绝与响应丢失不重放测试。测试曾发现
  新 fixture 的类型／import 编译问题，修复后纳入专项验证。
- 验证：全库 `go test -race ./... -count=1 -timeout 180s` 通过。取消／重启／权限／
  CLI／HTTP 专项五轮 race 通过；最终停止确认及错误映射调整后，Journal、Uploader、
  VFS、FUSE、MCP、控制面、daemon、CLI 八包整包 race 再次通过。最终 vet、本机构建、
  Linux amd64 构建及 gofmt 检查通过。现有环境仍缺 macFUSE/Fuse-T，不计真实内核／
  云盘／物理掉电验收；取消恢复测试的 Close/Open 也不冒充真实进程强杀或物理掉电。
- **边界未缩减**：停止不是远端撤销；无远端终态对账的取消记录仍不可普通 retry/drop。
  专用重试／对账／内容清理、在线 drop、MCP 上传管理入口、完整 Copy 终态历史及
  原子无覆盖等仍缺。其余新驱动、运行分发、UI/WebDAV/媒体、搜索规模优化与真实账号／
  keyring／内核／掉电／长期性能验收继续保留在整体目标中，本批不标记整体完成。

## 2026-09-05 第二十一批：取消上传的显式恢复与修订号保护

- 新增 CLI `uploads resume <id> --confirm` 与 `POST /uploads/resume`。没有明确
  确认、包含 all／分页等歧义参数时拒绝。在线使用原 daemon 的 VFS，不初始化第二套
  provider；离线只在日志存在时构建 NoBackground 栈，完成校验后排队，不启动上传。
  校验整文件默认 30 分钟，可调整 timeout；已连接后的响应丢失不会离线重放。
- Journal schema v9 保存每个上传的取消修订号。包括已经 cancelled 的重复取消在内，
  每个新停止请求都递增修订号；准备恢复时读快照并释放写锁，完整读取保留对象并验证
  CRC（power 模式也验证），拒绝缺失／非普通文件／越界私有路径／长度和校验不符。
  新停止、其他恢复或同 inode 后续提交会使旧准备的事务提交失败。
- VFS 校验当前 inode 的本地 UUID／版本／长度／名称、实际可写挂载和远端父 ID，
  在内容校验后重新检查目标。复制上传还核对原 CopySpec 的元数据库与挂载绑定。
  替换发生在校验前后都拒绝，不能让取消的旧版本覆盖已提交的新本地版本。
- 恢复是显式接受风险后的新上传尝试，不是自动远端对账。旧 multipart 会话可能已
  完成，故先把会话、分片和旧上传快照保存在私有历史，再清空当前 session／parts、
  重置尝试并进入 pending，所有步骤同事务提交，失败整体回滚。历史不暴露给普通
  列表，也不单独拥有 blob；历史查询、保留期限和回收策略仍待补。
- 普通旧上传没有原始账号身份指纹。当前 UUID／父 ID 与挂载检查不能证明账号从未
  更换；README 明确要求操作者在 confirm 前检查当前配置，说明可能重复或覆盖远端
  数据。不把人工接受重放风险描述为原子不覆盖或已经证明远端未生效。
- 新增 11 个测试：新的取消使旧恢复失效、全量内容损坏、会话／分片历史与新尝试、
  事务失败回滚、后续提交拦截、旧 v8 无修订号迁移；VFS 恢复后正常上传、只读／
  挂载根／新写入／校验中变化、复制原数据库绑定；CLI 在线使用原 owner 与离线不
  上传；HTTP 确认／浏览器拒绝。无确认参数与恢复响应丢失不重放也纳入既有回归。
- 验证：全库 `go test -race ./... -count=1 -timeout 180s` 通过；最终恢复／迁移／
  并发／CLI／HTTP 专项五轮 race 通过。最终内容读取边界调整后，Journal、VFS、
  Uploader、控制面、daemon、CLI 六包整包 race 再次通过；vet、本机构建、Linux
  amd64 构建和 gofmt 检查通过。真实账号／内核／物理掉电仍不计验收。
- **整体范围仍开放**：自动远端对账、在线 drop、本地版本销毁与历史回收、普通上传
  原账号绑定、完整 Copy 终态与跨端原子不覆盖、MCP 上传管理、新驱动、运行分发、
  UI/WebDAV/媒体、搜索规模优化及真实环境验收等未完成，未因本批恢复入口而缩减。

## 2026-09-05 第二十二批：MCP 缓存目录的有界分页

- 普通上传原账号绑定经检查仍未实现：凭据轮换与换绑不能仅用 remote 名称或
  token 哈希区分。本批没有写入推测的账号身份，也没有改变旧任务的恢复授权。
  此项与自动远端对账、在线 drop 等继续保持开放。
- Meta 新增 ChildrenPage，以既有 `(parent_ino, name)` 索引执行名称范围查询，
  LIMIT 限制节点加载量；支持旧数字偏移但不为它加载跳过的前缀。ChildrenCount
  提供独立索引计数，保留工具的 total 字段，不宣称 O(1) 或与页面同一快照。
  没有改变数据库 schema。
- VFS 新增 ReadDirPagePath，沿用目录 TTL、失败时提供已缓存旧列表、挂载解析和
  本地写入保护；只转换本页属性，最多 1000 项及一个判断后续是否有内容的节点。
  刷新只完成元数据合并，不再无条件读取所有缓存子项。并发完整 ReadDir 调用通过
  同一刷新结果的延迟加载共享一次完整查询；分页/lookup 调用不触发该加载。
- MCP 的 list_directory 改发名称游标，兼容规范十进制旧游标；修正原 Sscanf
  接受数字后尾随垃圾、在校验游标前访问 provider 的行为。资源保持现有 URI 游标
  格式，以最多 128 项窗口读取；按准确 JSON 编码长度累计预算，不再逐项重新
  序列化整个候选响应。权限、字节限额、URI 转义与错误脱敏保持原边界。
- 新增 8 个测试：存储层 Unicode 排序/分页/旧偏移/取消/参数校验、万项目录查询
  计划；VFS 冷/热/刷新失败、无完整缓存读取、删除后续页、虚拟根目录与 16 个
  并发读取者共享；MCP 1200 项工具/资源完整遍历、恶意游标先拒绝、旧游标兼容及
  删除后继续读取。已有资源字节预算、特殊字符、权限和 HTTP 认证测试继续通过。
- 验证：全库 `go test -race ./... -count=1 -timeout 180s` 通过（meta 128.505s）；
  Meta/VFS/MCP 分页与资源专项五轮 race 通过。最后补充延迟共享结果后，VFS/MCP
  整包 race 再次通过；vet、本机构建、Linux amd64 构建及 gofmt 检查通过。
- **未完成边界**：冷/过期目录的 provider 列举与 PutDir 仍全量聚合，需要流式
  暂存与原子发布才能解决超大冷目录内存；独立 total 的重复计数、大偏移旧游标、
  跨请求一致性快照及真实规模长期性能未解决。慢通知客户端隔离、SDK 长期回收、
  MCP 上传管理、上传/Copy 安全闭环、新驱动、服务分发、UI/WebDAV/媒体及真实
  账号/内核/掉电/POSIX 等原始剩余范围没有缩减，整体目标保持进行中。

## 2026-09-05 第二十三批：冷目录分页暂存与未上传后代保护

- VFS 冷/过期目录刷新不再跨 provider 页累计完整列表，改为按 200 项批次写入
  独立 SQLite TEMP 连接。每个 Store 最多 8 个暂存连接，普通元数据池不被网盘
  等待占满；TEMP 配置 FILE、2 MiB 缓存建议值和 spill，拒绝强制内存 TEMP 构建。
  关闭 Store 会关闭活动暂存；未提交集合不进入可见目录，也不是重启续跑的任务。
- 元数据库 schema v9 增加刷新代次，阻止先开始、后完成的旧刷新覆盖新刷新。
  暂存连接与提交事务检查数据库身份、目录 inode、remote/远端目录 ID；在另一
  Store 启动的更新刷新、删除/替换目标、数据库身份变化都会拒绝旧集合。
- 合并按有界批次读取暂存和当前旧节点，删除子树使用 TEMP inode 集合；最后一
  个事务更新节点、索引和目录完整性/刷新时间。游标在 TEMP 表去重，空页循环也
  不会无限请求；重复名称、非法名称、取消或暂存错误使集合失效，不能提交半份。
- 变更集合也暂存在连接内。小变更分批读取，大变更发送目录子树提示；挂载适配
  层把全缓存失效合并到一个 worker，遍历中发生的新请求不会丢失，也不按节点数
  创建 goroutine。提交结果不确定、通知读取失败时保守失效，不能把错误等同于
  持久化必然没有发生。没有实际内核环境，不宣称此分发层测试等同于真机通知验收。
- 发现并修复原目录合并只保护直接子项的问题：远端父目录消失时，清洁父节点下
  未上传文件的元数据可能被递归删除。传统 PutDir 与暂存合并现在都检查需要保留
  的后代。停用该保护后，两条存储路径均报 local descendant lost，VFS 读回报
  no such file；恢复后通过。本地内容保留不等于远端父目录被自动重建。
- 新增 13 个测试：完整合并/保护/有界变更、毒化暂存、末尾事务失败后的树/索引/
  TTL 回滚、跨 Store 代次与目标/数据库身份、连接槽位/关闭/超 TEMP 缓存建议值
  的暂存、后代保护及清洁子树删除、子进程强杀后重开；VFS 部分页失败但本地写
  可提交、空页游标循环、1200 项合并与单一大变更提示、待上传后代读回、提交错误
  的保守通知；1000 次重叠全缓存失效只同时运行一个遍历。
- 首轮新测试将“创建缺失本地文件触发了另一轮合法刷新”误当成本轮失败更新 TTL；
  已改为覆盖已有本地文件，使测试准确隔离分页期间的写入，不改生产语义来凑断言。
- 验证：全库普通测试与 `go test -race ./... -count=1 -timeout 240s` 通过（meta
  130.029s）。最终数据库身份/通知兜底补充后，专项五轮 race 通过，VFS/MCP/
  daemon/FUSE 四包整包 race 再次通过；vet、本机构建、Linux amd64 构建及 gofmt
  检查通过。强杀只验证进程终止后的目录一致性，不等于物理掉电验收。
- **未完成范围未缩减**：WebDAV/SFTP 等 provider 内部仍整目录返回；TEMP 不计入
  缓存空间配额，最终大事务写锁延迟、真实峰值内存、完整 ReadDir/warm/内核树遍历
  及跨请求快照仍需完善。上传账号绑定/对账/drop/历史清理、Copy 终态/原子不覆盖、
  MCP 上传管理和慢连接/SDK 回收、新驱动、服务分发、UI/WebDAV/媒体及真实环境
  验收继续开放。详细边界见 `docs/directory-refresh.md`。

## 2026-09-05 第二十四批：目录删除、改名与在途刷新互斥发布

- 确定性回归复现旧 provider 列表撤销已提交的本地操作：删除与改名后旧名称被
  插回，VFS 报 old listing resurrected；跨目录移动后，两边旧列表仍可提交。
  显式目录/全局失效和另一份传统 PutDir 合并也没有阻止旧暂存发布。
- 新增事务内刷新代次更新。Remove 保护父目录，Rename 同时保护源/目标父目录；
  同位置同名操作不更新代次。Invalidate、InvalidateAll、传统 PutDir 同样使相关
  在途暂存失效。未有刷新代次的目录不额外创建行，无关目录不被一起拒绝。
- 代次与节点/目录状态变更共用事务；后续节点写入失败时回滚代次。全局失效在
  代次更新本身失败时，也回滚已经执行的目录状态更新，不能留下半份失效结果。
  这不改变“已有完整目录时刷新失败可以提供当前缓存”的 VFS 策略；旧列表不会
  因此获得再次发布权限。
- 新增 7 个测试，覆盖本地/另一 Store 删除与改名、显式失效/传统合并、跨父目录
  覆盖移动、各类变更失败回滚、代次存储失败、无关目录与同名同位置改名、VFS
  删除/改名的确定性错序、待上传文件改名后的本地内容/journal 目标/排空后读回。
  首次新增上传排空断言有返回值数量编译错误，已按实际 Flush 签名修正并重跑。
- 验证：修复前上述竞争回归失败，修复后通过；全库
  `go test -race ./... -count=1 -timeout 240s` 通过（meta 128.076s），专项五轮
  race 通过。最后增加代次存储失败用例后，元数据专项五轮再次通过；vet、
  本机构建、Linux amd64 构建与 gofmt 检查通过。
- **范围保持开放**：此代次校验不产生远端跨客户端一致性快照；目录类型/远端
  对象替换的后代处理、单次完整 provider 枚举、TEMP 预算与大事务性能仍需完善。
  上传账号绑定/对账/drop/历史清理、Copy 终态/原子不覆盖、MCP 上传管理及慢连接/
  SDK 回收、新驱动、服务分发、UI/WebDAV/媒体和真实账号/内核/掉电/POSIX/长期
  性能等原始未完成模块未缩减，整体目标仍在进行中。

## 2026-09-05 第二十五批：目录对象替换、delta 后代与活动写句柄保护

- 核对工作树中已开始的目录替换修改，补齐存储/VFS 验收。传统 PutDir 和暂存
  合并在目录 remote/对象 ID 改变或文件/目录互换时，不再原地修改旧 inode。
  无受保护内容时，同一事务清除旧子树、目录状态与名称索引并插入新 inode；
  有受保护后代时保留整个旧对象，不把 journal 子项静默绑定到新的远端父 ID。
  仅目录版本更新保留原 inode/子树；复制绑定墓碑不随子树清理。
- 替换变更同时报告旧 inode 和消失/出现的名字，清 VFS 路径/目录 ID 缓存并
  失效内核 dentry/inode。每次替换按三项计入 128 项批次预算；大变更按原
  合并策略通知，旧 inode 已无法解析时 MCP 可退化为全量重新读取提示。
- 确定性复现 delta 删除只检查自身导致待上传后代 ENOENT；delta 类型更新复用
  目录 inode，甚至将未上传后代绑定到普通文件。新增 Meta.ApplyRemoteNode：
  在同一事务重读存储快照及 VFS 保护条件，已删除节点不复活，已变化快照拒绝，
  删除/替换复用后代保护和子树清理。成功推进父目录及相关目录刷新代次；失败
  不推进 delta 游标，保守失效，内容缓存不在元数据成功发布前主动丢弃。
- 继续复现已有文件的活动写句柄未提交时元数据仍干净，delta 删除/更新与列表
  替换都可能毁掉其原版本。VFS 新增按 inode 的写句柄计数，保护到最后一个
  close 提交结束，覆盖从 FH 表移除到本地发布之间的窗口。写打开查询/登记与
  远端发布通过短 RWMutex 协调；创建在查询后持锁重查父 inode。锁不跨 provider
  目录 IO；保护查询为计数点查，不逐次扫描全部句柄。只读句柄不保留清洁节点。
- 新增 16 个测试函数：两种列表路径的对象/类型替换、受保护后代、事务失败后的
  树/FTS/短索引/TTL 回滚、旧子目录刷新拒绝、版本兼容与通知预算；delta 快照
  竞争、变更刷新代次、原子回滚、待上传/新建未提交后代、类型替换、游标重试
  与失败通知；VFS 旧路径失效和新内容读回；活动写者、多句柄重复关闭及 close
  发布窗口。上述 delta 与活动写者回归在修复前分别确定失败，修复后通过。
- 验证：最终 `go test -race ./... -count=1 -timeout 240s` 全库通过（meta
  129.183s）；Meta/VFS 专项五轮 race 通过；`go vet ./...`、本机构建和 Linux
  amd64 交叉构建通过。新增测试期间发现未使用 import 和失效函数名称编译错误，
  按现有接口修正后重新验证。真实 FUSE 测试仍因缺少 macFUSE/Fuse-T 跳过，
  单元/用户态 VFS 通知不代替实际内核、真实账号或物理掉电验收。
- 文档校准：TODO 顶部旧基线改标历史记录，UNVERIFIED 实际为 51 处，修正百度/
  夸克计数；复制文档元数据库版本改为 v9，移除授权/准备管理仍完全缺失的旧描述。
- **整体仍未完成**：保护活动写句柄会延迟外部变更应用，关闭后仍依赖 TTL/冲突
  检查，不自动恢复已删除的远端目录。普通文件到普通文件的 ID 替换仍沿用旧
  inode 策略，provider 分页不提供跨客户端快照，任意祖先/后代操作的全树并发
  语义仍需审查。上传原账号绑定、远端对账、在线 drop/本地版本与历史清理、
  Copy 完整终态/原子无覆盖/准备目标改名、MCP 上传管理和慢客户端/SDK 回收、
  provider 流式枚举/TEMP 预算/规模性能、新驱动、部署分发、UI/WebDAV/媒体、
  真实环境验收及许可证决策等原始目标继续保持开放。

## 2026-09-05 第二十六批：WebDAV 流式枚举与资源 URL 身份修复

- 新增 Caps.StreamList 与可选 StreamLister。VFS 按能力矩阵选择逐条交付，使用
  同一 200 项 TEMP 收集器；缺少能力时保留分页/游标去重。只有未交付任何条目
  的 ErrUnsupported 才可回退，前缀已交付后禁止重放。记录并传播本地回调错误，
  即使驱动忽略错误并返回成功也不发布。取消、解码失败和存储错误不改变旧目录。
- 计数包装的普通/change/copy/both/single-put 变体均转发流式接口。无底层能力的
  探测返回 ErrUnsupported 且不统计网络调用；真实流式枚举仍计作 list，耗时包含
  下游回调处理，不把它表述为纯 HTTP 往返耗时。
- WebDAV/OpenList 的 ListStream 使用共享 HTTP 客户端的 Stream 模式逐条解析
  DAV:response。HTTP 状态成功后不对已开始读取的响应重试，关闭响应体覆盖成功、
  回调错误和取消。单元素/忽略扩展/巨型 token 有 1 MiB 读取预算（另有解析器
  缓冲及对象开销），目录整体不受此数量限制；兼容 List 仍按接口约定累计切片。
- 完整 XML 收尾前不确认目录，拒绝错误根/命名空间、截断、额外文档、过大元素、
  缺 href、属性全部失败、错误父路径和负长度。成员无法验证不是成员不存在，
  不能因此删除已有缓存子树。Stat 的 Depth:0 完整解码仍未改成流式路径。
- 新测试确定性复现旧 href 先 unescape 再 Parse 会截断 #/?，把字面 %2F 二次
  解码成路径分隔符。改为先解析 URL，再使用单次解码的 Path，并校验配置根边界。
  测试覆盖 #、?、字面 %2F、空格/中文和 &；首次 & 样例漏了 href 的 XML 转义，
  已修正 fixture，不通过宽松 XML 解码来容忍错误输入。
- 新增 9 个测试函数：真实 HTTP 服务阻塞尾部直到首项回调，随后发送 5000 项；
  回调错误/取消关闭连接且仅一次请求；XML 完整性和单元素预算；不完整属性等
  成员拒绝；转义 ID；包装层能力/计数；VFS 七种流式/回退/失败模式、前缀不可见、
  1200 项完整发布；真实 WebDAV→计数包装→VFS 冷分页及截断后旧缓存保留。
- 验证：最终 `go test -race ./... -count=1 -timeout 240s` 全库通过（meta
  133.474s），三包专项五轮 race 通过；vet、本机构建、Linux amd64 交叉构建及
  gofmt 通过。此前一轮全库测试发现新转义测试 fixture 的 & 未做 XML 转义，
  修正后重新全库验证通过；没有把那一轮失败记为通过。实际 FUSE/云盘/长期
  性能仍不能用 HTTP 模拟服务及用户态测试替代。
- **原始目标继续开放**：SFTP 的 pkg/sftp v1.13.11 公开 ReadDir/ReadDirContext
  均聚合切片，底层分包不等于 API 有界交付；没有包装全量数组来假报流式能力，
  SFTP 传输适配仍需开发。TEMP 配额、最终大事务锁延迟、全量兼容入口/真实内存
  与长期性能、跨客户端一致性，以及上传账号绑定/远端对账/drop/历史清理、Copy
  完整终态/原子不覆盖、MCP 管理/通知隔离、新驱动、部署分发、UI/WebDAV 服务端/
  媒体和所有真实环境验收等仍未完成。本批不改变 provider 云盘 UNVERIFIED 标记。

## 2026-09-05 第二十七批：SFTP v3 分包目录读取器

- 核对当前 pkg/sftp 的公开接口及连接池，确认不能把 ReadDirContext 的完整
  切片包装成真正的流式枚举。新增独立目录协议读取器，不修改/复制第三方模块，
  使用独占、已认证的 sftp 子系统流；不与现有 pkg/sftp 活动通道混发协议。
- 实现 INIT/VERSION、OPENDIR、READDIR、STAT 补全属性和 CLOSE。严格检查包
  长度/类型/请求 ID、名称、属性标志与计数、大小溢出和尾部；单包分配前限制
  为 1 MiB。忽略 . / .. 及在途上传名；一个条目一次回调，不累计完整目录。
  正常结束要求 SSH_FX_EOF 及 CLOSE 成功应答，传输 EOF/空 NAME/失败状态
  不能被当成目录完整。缺少类型/大小/mtime 时补查，不猜成零长度文件。
- 访问者错误不重试前缀。取消关闭独占流，覆盖协议初始化/打开/读取/属性补查/关闭
  所有阻塞点；退出等待本次取消回调结束，避免回调在调用返回后继续操作旧流。
- 新增 6 个测试函数和 1 个 fuzz 入口。真实 pkg/sftp 服务端经 net.Pipe 验证
  513 个文件、一个子目录及隐藏上传文件；脚本报文验证版本/状态/ID/长度/属性/
  名称/关闭失败和部分内容后 EOF；覆盖 STAT 补查、访问者中止、五阶段取消、
  字段截断/超大帧和短写。协议专项五轮 race 通过；10 秒、2 worker 字段模糊
  测试执行 758236 次，未发现失败。该次数不代表覆盖所有恶意输入。
- 验证收尾：`go test -race ./... -count=1 -timeout 240s` 全库通过（meta
  128.482s），vet、本机构建、Linux amd64 构建及 gofmt 通过。真实 SSH/账号/
  NAS/内核/物理掉电仍不计验收；管道上的 SFTP 协议服务端不代替这些环境。
- **尚未接线，不标记完成**：当前生产 SFTP List 仍完整返回，StreamList 仍为
  false。后续必须实现继承代理/认证/主机密钥/限流配置的独占通道获取、并发槽位、
  连接复用与取消清理，接入 Provider/VFS 后再做真实 SSH 链路及半途失败验收。
  具体要求见 `sftp-directory-stream.md`。本协议基础不是更小目标的完成替代物；
  TEMP 配额、规模性能、上传/Copy 安全闭环、MCP 剩余、新驱动、部署分发、产品
  入口及全部原始真实环境验收范围继续保持开放。

## 2026-09-05 第二十八批：SFTP 生产流式连接与 VFS 验收

- 上轮现状分析没有新增实现。本轮将独立 v3 读取器接入生产 ListStream，能力
  矩阵启用 StreamList，计数包装和 VFS 自动采用逐条 TEMP 收集路径。兼容 List
  调用同一读取器后累计切片；注入现成 pkg/sftp Client 的测试模式保留旧接口和
  非流式能力。没有引入依赖 fork，也不把回调包装的完整 ReadDir 当作流式。
- 新增有界、可复用的专用 SSH 池，继承现有 Dial/代理、账号认证、known_hosts、
  HostKeyAlias、密钥算法和元数据限流。`directory_connections` 默认额外 2 条、
  上限 32，与 `connections` 相加才是总连接上限，默认最多 4 条，均按需建立。
  列举取消只关闭专用传输，不清除普通文件句柄。相对根在同一子系统用 REALPATH
  解析并验证唯一绝对路径；不调用共享文件连接 Getwd，不运行 shell。
- 拨号/SSH 握手/打开通道/启动子系统受设置期限和取消控制；设置完成撤销设置
  计时，不限制整个大目录为 15 秒。每次子系统使用两个排空循环，关闭写入/等待
  受 2 秒期限约束，必要时关闭 TCP 后等待读取循环退出。租约归还前等待旧取消
  回调退出；关闭后迟到的拨号连接不被发布。空闲死连接仅在协议开始前重连一次，
  已交付条目、协议初始化失败或最终 CLOSE 失败均不重放前缀。
- 回归先复现再修复：初版将元数据限流放在池外，Provider.Close 无法唤醒等待者，
  测试在 4 秒结束门槛失败；现在限流等待与槽位、网络一样受关闭信号管理。
  另发现原连接地址用字符串拼接，IPv6 得到无方括号的 `2001:db8::42:2222`；
  新测试先失败，再改用 net.JoinHostPort，两个池的拨号与密钥算法地址一致。
- 新增 16 个测试函数和加密回环基准：真实 SSH 密码/主机密钥校验、子系统拒绝、
  六种根配置、513 文件及子目录、连接复用、十阶段取消、2 槽位与多等待者、
  设置期限/长扫描、旧取消 50 次不影响新租约、空闲重连、迟到拨号、限流与关闭、
  无响应关闭、错误身份及不重放、REALPATH 畸形返回和 IPv6 构造。
- 普通文件句柄在列举前/中/取消后保持读回一致，句柄计数不变。VFS 集成通过
  Instrument 使用 ListStream，禁用能力即复现失败（拒绝兼容全量 List）；500 项
  完整发布后，第二次输出 300 个不同条目再断线，旧 500 项和刷新时间不变，
  流式调用总计 2 次，没有半份发布或兼容回退。
- 基准使用真实加密回环 SSH、每包动态生成 100 项、先热身连接，1 次/轮共 3 轮。
  十万项流式中位 76.4 ms、累计分配约 87.2 MB；兼容完整 List 中位 78.8 ms、
  累计约 141.5 MB。包含服务端构造和两端加密成本，不是峰值内存、NAS 吞吐或
  稳定性能门禁。代码、命令与环境记录于 `sftp-directory-stream.md`。
- 验证收尾：包含最终 IPv6 增量的 `go test -race ./... -count=1 -timeout 240s`
  全库通过（meta 128.011s）；SFTP SSH 专项五轮 race 通过。字段 fuzz 10 秒、
  2 worker 共执行 246103 次无失败，保留 13 个种子/覆盖输入；这不是穷尽证明。
  `go vet ./...`、本机构建、Linux amd64 构建和 gofmt 检查通过。单独重查真实
  FUSE 与 live SFTP 探针，分别因无 macFUSE/Fuse-T、未配置 CLOUDFS_LIVE_SFTP
  跳过，不计作真机验收。非测试 UNVERIFIED 仍为 51，未因回环测试删除标记。
- **整体仍未完成**：SFTP 生产流式接线已实现，但真实 NAS/SSH-agent/账号/内核
  验收、峰值内存、TEMP 配额、大事务与其他全量入口仍待补。普通文件连接原有
  初始化/根缓存路径不因此具有同等取消保证。上传/复制远端对账与安全清理、
  MCP 剩余、其他驱动、部署分发、产品入口和原始全部真实环境验收继续开放。

## 2026-09-05 第二十九批：上传清理的持久化基础与防复活

- 上轮 SFTP 生产流式接线为实际开发进展。本轮回到在线 drop 的安全前置条件，
  实现 journal v10 的 upload_cleanup / upload_discarded。仅已 cancelled 且非
  tombstone 补偿的上传可进入 purging；意图记录元数据库身份与规范化私有路径。
  原上传行与 session/parts/history 在完成清理前继续保留；普通 Commit 不得凭空
  构造没有意图的 purging 行。旧 v9 只读查看不迁移，升级仍需 journal owner。
- Begin 原子锁定停止状态；Finish 要求调用方已完成 VFS 引用移除/FULL 屏障和
  缓存 checked unlink/fsync，然后才处理私有 payload。objectMu → writer 锁覆盖
  已 staging 未入队的引用；其他上传按流式扫描检查规范化路径和 inode 别名。
  共享 owner 仍存在时保留数据，已 submitted Copy 的源硬链接与准备历史不被删除。
- 私有路径、直接父目录及文件类型检查拒绝外部路径、被改写的路径、目录和符号
  链接。unlink/同步窗口/最终 SQL 失败留下可重试意图；已 unlink 后重启也能完成。
  Finish 在同一事务删除会话/分片/取消/恢复历史与上传记录，并保存最小 ID 凭据，
  重复完成不重新删除文件，错误的元数据库身份不能继续清理。
- 丢弃 ID 不能被迟到 Commit、Succeed、Fail、MarkPublished 或分片/session 回执
  复活。既有恢复与清理竞争只有一个事务赢家；相同内容重新 staging 成新上传
  可以成功，不能靠新 ID 直接引用已经丢弃且缺失的旧 payload。
- 回归先验证两个缺口：关闭分片防护后，删除记录后的 RecordPart 返回成功并
  重建孤立记录；新增 purging 后，原 Flush 对只有清理记录的队列返回 nil。
  两者均已修复。普通 namespace 的 ByIno 查询包括 purging，改名/删除不能绕过；
  strict Flush/FUSE/MCP/控制 HTTP 都有对应错误或 EBUSY/冲突，不误报远端成功。
- status、metrics、doctor 与 CLI status 展示清理数量及警告，不向管理响应输出
  meta identity、blob 路径或 session；doctor 不提供自动 retry 修复。逻辑保留字节
  不是实际磁盘占用，清理凭据暂不 GC，SQL 删除不承诺擦除 WAL/空闲页/备份。
- 新增 16 个测试函数（含一个子进程辅助入口和收尾发现旧 GC 别名问题的失败回归），覆盖状态/路径/身份/只读/迁移、
  SQL 回滚、共享与未提交引用、恢复竞争、历史删除与 ID 防复活、复制源硬链接、
  查询告警/错误映射和普通 namespace 拒绝。子进程分别在意图落盘后、unlink 后、
  最终提交前被强杀，父进程重开 Journal、验证未重传并续清理，不复用子进程内存。
  初版 HTTP 测试遗漏 Content-Type 得到正确的 415，修正测试请求后验证 409，
  未放宽生产请求校验。
- 验证：首轮全库 race 通过（meta 142.072s）；随后收尾增加的旧 GC 别名回归
  失败，因而本批最终状态不能记作全绿。下一批修复及全库验证记录见第三十批。
- **没有开放不完整的在线 drop**：当前生产管理入口不调用 Begin/Finish，VFS
  当前版本删除/别名与打开句柄保护、FULL 屏障、缓存清理、daemon 启动续清理、
  CLI/control 的统一确认与故障处理仍必须接入。Journal 的 metaIdentity 参数
  不能自行证明本地引用消失。详见 `upload-cleanup.md`，本批不是在线丢弃或完整
  历史回收完成；其余原始模块、远端对账、真实账号/内核/物理掉电及规模验收保持开放。

## 2026-09-05 第三十批：旧 GC 别名误删修复与本地版本持久删除

- 上轮只读分析复跑现有失败回归，确认 removeBlobIfUnreferenced 使用原始路径
  字符串计数，无法识别同一对象的相对/绝对路径。删除其他任务时可能删除 purging
  任务仍保留的 payload。此次普通 Drop/DropPending 与新清理共用逐行规范路径/
  inode 引用检查，保留 staging 与 copy owner 检查、objectMu → writer 锁顺序。
  读取引用身份失败时保留数据。修复前专项与全库均失败，修复后原用例通过。
- 新增普通删除矩阵：pending/uploading/done/dead/cancelling/cancelled/purging
  七种 owner 状态 × 两种相对/绝对路径方向 × Drop/DropPending 两个入口；
  另覆盖符号链接父路径的 inode 别名和 ELOOP 引用错误。两项测试函数及原有
  上传清理/删除专项连续三轮 race 通过；随后全库 race 通过（meta 128.102s）。
  此结果是下一项元数据增量之前的基线，不混作最终代码验收。
- 新增内部 Store.RemoveLocalVersion：以原数据库身份和全部本地节点快照为
  删除依据，拒绝漏掉的别名、并发改名/替换、异常其他 remote 引用及文件下的
  子节点。精确文件删除、搜索记录清理和父目录刷新代次在一个 FULL 事务内完成。
  保留 copy_bindings 防复活墓碑；零节点重试仍真实切换持久化屏障，缺失屏障行
  返回错误并回滚，不能把无实际写入的 FULL 事务当作删除已经同步。
- 元数据四项测试覆盖授权边界、两个故障窗口完整回滚、搜索/负缓存一致性、
  重开与重试以及独立 SQLite 连接并发改名的唯一赢家。专项三轮 race 通过。
  负对照临时移除目录代次更新，旧列表提交返回成功并复活内容；恢复代码后继续
  验证。初版测试误把 IsAbsent 的两个返回值当作一个，已修正测试调用，未改 API。
- 最终验证：包含元数据增量的 `go test -race ./... -count=1 -timeout 240s`
  全库通过（meta 130.214s）；`go vet ./...`、`go build ./...` 与
  `GOOS=linux GOARCH=amd64 go build ./...` 通过，改动 Go 文件 gofmt 干净。
  恢复负对照代码后的相关专项另复跑通过。真实挂载专项再次明确因本机未安装
  macFUSE/Fuse-T 跳过，未把交叉构建或包显示 ok 当作内核验收。
- **仍未完成**：元数据方法不是用户删除入口；上传清理的 VFS 句柄/挂载权限
  协调、缓存清理、daemon 恢复及 CLI/control 确认入口仍需接线。新引用检查和
  全量别名快照仍有大队列/大元数据库开销，未宣称规模性能完成。普通上传原账号
  绑定、远端对账、其他驱动、部署分发、产品入口和原始真实环境验收继续开放。

## 2026-09-05 第三十一批：VFS 上传清理协调与 daemon 启动续清理

- 上轮共享内容误删修复和原子元数据删除为实际开发进展。本轮接入
  FS.DiscardUpload：要求 owner、确认、cancelled 完整发布、原 inode 当前版本，
  校验全部别名及可写挂载/父目录；submitted Copy 额外校验原数据库/目标挂载。
  冻结 journal 意图后，依次执行 FULL 本地版本移除、缓存 checked unlink/fsync、
  Journal 最终删除，任何失败保留 purging。没有 provider 调用，也不撤销远端结果。
- copyPublishMu 改为读写门，读 Open 加共享准入，清理排除新读者与 namespace/
  Copy/resume 发布，再排除 Create/远端刷新。已关闭写者保留到提交返回；新增
  读者初始内容身份和在途 Read 计数，Release 不提前移除仍有读请求的句柄，
  已关闭句柄不接受新 Read。负对照恢复“Release 立即移除句柄”后，专项复现
  在途读取报 local-only cache missing；恢复保护后继续最终验证。
- 进一步核对发现 MCP ReadFileRange 和 pin 下载原先手工构造临时句柄，绕过
  Open 登记。两者已走统一 Open/Release，路径读取使用 Read 并补确定性在途
  路径读者拒绝清理回归，不能只靠 FUSE 打开测试证明所有读取入口安全。
- 元数据移除成功后立即通知所有被删除别名并清路径缓存，后续 unlink 失败也
  不掩盖已发生的目录变化。释放该版本临时 pin/用户缓存标志，保留持久路径规则。
  独立完整缓存租约在私有名称删除后仍可读、继续计量至 Close；已提交复制的
  源硬链接和准备 submitted 历史保留，准备记录仍由独立 forget 管理。
- daemon 在 Journal.Recover 后、RecoverPublications 和任何上传/刷新 worker
  前续清理已有意图，按 200 条分页扫描；不是自动取消其他上传。身份不匹配或
  清理失败阻止启动并保留意图，不能跳过错误后启动上传。未持 owner 不续清理。
- 新增 13 个测试函数（含一个子进程辅助入口），覆盖确认/状态/挂载/别名/读写
  句柄拒绝、关闭中读者和提交中写者、读打开与删除竞争、MCP 路径读取、共享
  内容和 pin/租约、submitted Copy、数据库替换、SQL/缓存 unlink/各持久化边界
  失败续清理。VFS 子进程在 intent/metadata/cache 三阶段被强杀，父进程重建
  所有组件并续清理，不复用子进程堆。daemon 装配测试复核相同三个启动边界。
  首轮测试错误调用了不存在的 Journal.DB，已改用真实 resume/claim/cancel
  状态转换；provider 调用断言使用装配后的 CallStats，避免对包装类型跳过断言。
- 最终验证：早期接线全库 race 通过（meta 132.671s），专项三轮通过；随后
  路径读取/pin 登记修正的 VFS/daemon/MCP 整包 race 通过。包含全部最终代码与
  测试的 `go test -race ./... -count=1 -timeout 240s` 全库通过（meta 134.599s）；
  `go vet ./...`、本机构建、Linux amd64 交叉构建通过，改动 Go 文件 gofmt 干净。
  真实 FUSE 冒烟再次因本机没有 macFUSE/Fuse-T 跳过，不计作真机验收。
- **尚未开放在线 drop**：CLI/control 的统一确认入口、响应丢失不重放和生产
  CLI 强杀验收待补；离线旧 DropPending 仍不是上述安全流程。首次已替换或
  缺失版本的历史清理、普通上传原账号/元数据库绑定、远端对账、完整历史策略
  及规模开销仍开放。其他驱动、分发、产品入口与真实账号/内核/物理掉电等原始
  目标保持进行中，不以内部协调器验收替代整项管理功能或全部项目完成。

## 2026-09-05 第三十二批：CLI/control 上传安全丢弃与生产进程恢复

- 在线 `POST /uploads/drop` 与 CLI `uploads drop <id> --confirm` 已调用上一批
  VFS 清理协调器；旧的 Journal-only DropPending 管理分支移除。请求只接受一个
  规范 JSON 对象 `id+confirm`，拒绝重复/大小写字段、其他选择器、查询参数、浏览器
  来源和非本地主机。成功返回 discarded ID 和本地/远端边界，不伪装为 done。
- CLI 建立控制连接后的超时、断开或响应丢失不转离线重放。明确不可连接时，离线
  流程先检查既有 journal/meta 文件并取得 owner，再构造 cleanup-only VFS；不初始化
  provider、凭据、代理，不创建挂载节点，不运行 Journal.Recover、发布恢复或其他上传。
  元数据离线选项还关闭后台索引器及 Close 刷新，不把普通 schema 迁移误称为零写入。
- 生产进程专项实际编译 cloudfs，通过 Unix 控制端点开始清理，并用独立 SQLite 写
  事务阻塞元数据阶段；观察 purging 落盘后强杀 CLI 和 MCP-only daemon，再启动新
  生产 daemon。原 inode、payload、缓存链接最终消失，WebDAV 请求计数不增加，证明
  中断没有重放上传或误报远端成功；后端仍是本地 HTTP 服务，不替代物理掉电/真账号。
- 新增 control/CLI/meta/生产进程回归，覆盖 14 类歧义请求、句柄冲突、内部错误脱敏、
  在线不回退、离线所有权和不恢复其他任务、purging 重试、配置变化不建新挂载节点。
  专项三轮 race 通过。最终全库 race 通过（meta 134.039s）；vet、本机构建及 Linux
  amd64 交叉构建通过。FUSE 仍因本机缺 macFUSE/Fuse-T 跳过，不计真机验收。
- **剩余范围不缩减**：首次已替换/缺失的历史版本、普通上传原账号绑定、自动远端
  对账、完整历史保留/凭据 GC 和规模开销仍开放；MCP 上传管理、Copy 剩余安全闭环、
  驱动、部署、产品入口和全部真实环境验收继续进行。

## 2026-09-05 第三十三批：MCP 上传管理与路径权限绑定

- 新增 `list_uploads/get_upload/retry_upload/cancel_upload/resume_upload/
  discard_upload/flush_uploads`，工具数由 22 增至 29。查询只投影 ID、当前可验证路径、
  remote、名称、状态、大小、尝试和重试时间；不返回 blob、hash、provider session、
  预期远端版本或原始 backend/SQLite 错误。错误统一脱敏，统计使用稳定 JSON 字段。
- VFS 新增安全上传投影和管理包装。路径只有在 inode、local ID/version、名称、长度、
  remote、挂载及父目录对象全部仍匹配时才给出；替换/缺失/tombstone/purging 不猜路径。
  受限 MCP 的 retry/cancel/resume 在 publication gate 内再次核对调用时授权路径，
  其中可能很慢的 retained-content CRC 在锁外完成，提交前再核对，避免阻塞无关读取。
- 列表原始扫描每页最多 200 条，并受 MaxEntries/MaxBytes 双重限制。允许列表过滤后
  可以是空页但仍给续页；游标认证加密、不暴露隐藏 upload ID、不可篡改或跨服务重用。
  全队列 flush 仅向无允许列表限制的服务开放。discard 同样仅无限制服务可用，因为
  元数据阶段完成后重试可能已无路径可重新证明授权；两者和其他修改均受只读门禁。
- 回归覆盖 in-memory 和带 bearer 认证的真实 Streamable HTTP MCP、工具注册、敏感
  错误不泄露、路径过滤、隐藏 201 条的有界空页、游标篡改、只读/确认、retry/cancel/
  resume 状态、实际本地 discard 与 flush。VFS 负对照覆盖错误预期路径不改变 journal，
  以及 CRC 窗口发生版本替换后最终提交拒绝。专项三轮 race、vet 和双平台构建通过。
  包含最终代码的全库 race 通过（meta 132.153s），gofmt 检查干净。真账号/FUSE/
  物理掉电不由这些测试替代；本机 FUSE 专项仍因缺 macFUSE/Fuse-T 跳过。
- **整体仍未完成**：上传原账号/元数据库绑定、未知远端结果自动对账、历史版本和凭据
  GC；Copy 远端对账/跨端不覆盖/目标改名；MCP 慢连接及 SDK 长期回收、TEMP 配额与
  大事务；新驱动、部署分发、产品入口及真实环境验收仍保持开放。

## 2026-09-05 第三十四批：上传原数据库、挂载与账号代次围栏

- 配置 remote 新增本地 `account_binding` 授权代次。config add 自动生成；显式
  config auth 每次换代；自动 token 保存保持代次。旧明文配置以未解析的类型和 Extra
  生成兼容绑定，并在首次 token 轮换或 `auth --migrate` 改写密钥引用前冻结，因此
  存储迁移不误判换账号。新代次的运行时绑定同时哈希 remote 类型及非密钥账号定位
  配置，手工改 endpoint/principal 也会停住队列，密钥值轮换不会。
- journal schema 升至 v11，Upload 行增加原元数据库身份、挂载前缀、根对象和账号
  绑定。普通写入在 blob 提交后、日志提交前取绑定；Copy 在原子 Submit 前取目标绑定。
  四字段要求全有或全无，防止写出半绑定记录。只读端可查看 v10 并投影为空，owner
  打开才迁移；旧记录不被猜测补值。
- Uploader 领取任务后先执行 VFS 授权钩子，再检查版本或解析 provider。数据库、
  挂载前缀、根对象、remote 或账号绑定不符，以及 v10 的空绑定任务，均 fail closed
  为死信；本地 payload 与 local-only 版本保留，provider resolver 和远端请求计数为零。
  daemon 真实装配重启测试在同名 remote/root 改授权代次后验证相同结果。
- 已取消任务的显式 resume 仍要求 confirm、当前 inode/name/size/local version、
  可写挂载、父对象及完整 CRC 两阶段复核；最终事务用当前四项绑定重新接纳，同时把
  旧绑定、session 和 parts 保存到私有恢复历史。这样旧 schema 或经人工核对的换绑
  内容有明确迁移路径，但不把人工确认描述成远端对账。
- 新增配置代次/定位指纹/轮换/迁移、journal v10 只读与 owner 迁移、绑定往返/
  半绑定拒绝/恢复历史、普通写入、数据库/前缀/根/账号/只读/嵌套遮蔽六类漂移、
  provider 解析顺序、daemon 重启及确认恢复回归。专项三轮 race 通过；最终全库
  `go test -race ./... -count=1 -timeout 240s` 通过（meta 136.646s，VFS 36.414s）。
  `go vet ./...`、本机构建、Linux amd64 交叉构建及全库 gofmt 检查通过。FUSE
  显式专项再次因本机未安装 macFUSE/Fuse-T 跳过，未计作内核挂载验收。
- **剩余范围不缩减**：该围栏证明“任务仍处于接受写入的本地配置”，不证明云端
  已执行或未执行，也不提供 provider 签发的账号身份。Copy 下载准备的源账号代次、
  未知远端结果对账、历史版本/凭据 GC、跨端无覆盖、目标改名，以及驱动、部署、
  产品入口和真实账号/内核/物理掉电/规模验收继续开放。

## 2026-09-05 第三十五批：Copy 准备源/目标账号代次绑定

- CopySpec 的持久 JSON 增加源、目标账号绑定。VFS 创建准备任务时同时记录元数据库、
  两端挂载前缀/根对象及账号代次；半绑定新作业在 Journal 准入时拒绝，旧 JSON
  缺字段仍可只读加载但不被当前 VFS 猜测接纳。
- 未完成下载的 ResumeCopy 和显式 RetryCopy 在任何源 provider Stat/Read 前核对
  源路径当前有效挂载及账号代次；所有 retry/ready 发布在创建本地目标和上传交接前
  核对目标账号代次。完整 ready payload 不再依赖源读取，但目标仍 fail closed。
  Copy 列表/MCP 权限投影也把两端账号纳入可见性，旧账号作业不可仅凭 ID 操作。
- 已交接 Upload 继续使用 v11 的目标账号绑定；本地-only discard 不因账号换代被
  禁止，因为它不调用 provider，仍按原元数据库、路径、挂载根和显式确认约束执行。
- 回归覆盖绑定 JSON 往返/半绑定拒绝、普通 Copy 两端持久化、源/目标换绑保留检查点
  且零 provider IO、旧空绑定作业隐藏、嵌套/账号变化的 MCP 过滤，以及 control/
  daemon/CLI 夹具的真实 VFS 管理路径。六个相关包三轮 race 通过（VFS 176.976s，
  MCP 98.501s）；最终全库 `go test -race ./... -count=1 -timeout 300s` 通过
  （meta 130.573s，VFS 42.832s）。vet、本机构建、Linux amd64 交叉构建和全库
  gofmt 检查通过；本批不重复把缺少 FUSE 驱动的跳过项计作真机验收。
- **仍未完成**：本地代次不等于 provider 签发的云端身份；服务端 Copy 响应丢失
  对账、跨客户端条件创建、准备目标改名、完整终态历史/GC 和真实账号故障矩阵仍开放。

## 2026-09-05 第三十六批：systemd / launchd 用户服务管理

- CLI 新增 `service install|uninstall|status`，只读取配置，不构建 daemon/provider。
  install 固化当前可执行文件、配置与第一个 mount 的绝对路径；Linux 写
  `~/.config/systemd/user/cloudfs.service`，reload 后 enable --now；macOS 写
  `~/Library/LaunchAgents/io.cloudfs.mount.plist` 并 bootstrap 当前 GUI domain。
- systemd 定义包含 network-online 顺序、RequiresMountsFor、Restart=on-failure、
  RestartSec 和停止期限；launchd 定义包含 RunAtLoad 及 SuccessfulExit=false 的
  KeepAlive。参数不经过 shell，分别做 systemd 引号/美元/百分号转义和 XML 转义。
  定义通过同目录临时文件、fsync、rename、目录 fsync 原子安装，权限 0644。
- uninstall 先停用 supervisor；cloudfs 收到 SIGTERM 会走既有正常卸载，再用设备
  边界检查发现被强杀留下的 FUSE mount 并显式 umount，成功后删除定义，Linux 再
  daemon-reload。manager 停止或残留卸载失败时保留定义并返回错误，不制造“已卸载”。
  配置、cache、journal 和普通目录不删除。
- 单元测试同时生成 Linux/macOS 定义，覆盖空格、引号、美元、百分号与 XML 字符、
  RunAtLoad/KeepAlive/Restart/RequiresMountsFor、安装/卸载命令顺序、status 输出、
  0644 文件、失败后定义保留及残留挂载清理。真实 service manager 未在测试中调用。
- service 相关 CLI race 五轮通过，macOS 原生 `plutil -lint` 接受生成的 plist；
  最终全库 `go test -race ./... -count=1 -timeout 300s` 通过（meta 131.518s，
  VFS 42.368s）。vet、本机构建、Linux amd64 交叉构建和全库 gofmt 检查通过。
- **边界**：本机没有 FUSE 驱动，无法执行 reboot 或 kill -9 后自动重启挂载验收；
  Linux 无人登录运行还取决于
  发行版 user manager 与 linger 策略；当时 Docker/CI/release 仍是独立分发缺口，
  后续进展见第三十七批。

## 2026-09-05 第三十七批：容器、CI 与 tag release

- 新增无 CGO 的 Go 1.27 多阶段镜像；Debian runtime 含 CA、时区与 fuse3，默认 uid/gid
  65532，MCP-only 不需要设备或 capability。Compose 把 MCP-only 和 Linux FUSE 分成
  互斥 profile；后者显式声明 `/dev/fuse`、`SYS_ADMIN`、AppArmor 例外及 rshared 输出，
  不隐藏成 privileged entrypoint，control 端口也不绕过既有回环安全边界。
- 非回环 MCP HTTP 从 `CLOUDFS_MCP_TOKEN` 接入已有 bearer 验证，环境变量缺失继续拒绝
  启动。修复 `mcp --http 0.0.0.0:8765` 中 optional-value flag 被当成纯 bool、地址落入
  位置参数的问题，并加解析回归。
- Makefile 与发布脚本统一使用 `CGO_ENABLED=0`、trimpath、空 build id 和 linker 版本；
  本地实跑生成 linux/darwin × amd64/arm64 四个二进制和 SHA-256 checksum，darwin/arm64
  资产执行 `version` 正确报告 0.1.0。脚本拒绝覆盖已有 dist，避免旧资产混入。
- GitHub CI 在 Linux/macOS 执行 format、vet、全库 race 和静态构建，Linux 另做真实镜像
  version/doctor 烟测。semver tag workflow 重跑门禁，先建 draft 附件 release，再发布
  GHCR linux/amd64+arm64 镜像，成功后才公开 release。
- 本地 Docker Compose 5.4 已接受两个 profile 的展开配置，示例 config 通过 cloudfs
  自身校验；本机 Docker daemon 未运行，所以没有把 Dockerfile build/doctor 记为通过，
  GitHub tag、GHCR、Linux FUSE 与包管理器同样保持外部验收。许可证仍由所有者决定。
- 收尾验证：actionlint v1.7.12（含 shellcheck）接受两份 workflow；全库普通测试、vet、
  native 及 linux/amd64、linux/arm64、darwin/amd64 静态构建、gofmt 检查全部通过。
  最终 `go test -race ./... -count=1 -timeout 300s` 通过（meta 136.523s，VFS 43.170s）。

## 2026-09-05 第三十八批：内嵌只读状态页

- `control.ui` 默认开启且可显式关闭；CLI 只在配置了 `control.metrics` 时打印 dashboard
  URL。页面嵌入二进制并复用同一个 loopback-only control mux，不增加端口、数据库查询
  或绕过 Collector/VFS 的数据路径。MCP-only 的 stdio 仍不输出 control 提示。
- 单页每五秒读取既有 `/status`，展示缓存命中与淘汰、上传活动/死信、元数据、挂载、
  remote 调用/流量/熔断、代理健康和 warnings；所有远端字符串使用 DOM `textContent`，
  不执行服务端返回的 HTML。上传列表和 retry/cancel/resume/drop 复用既有 control API；
  resume 显示远端重放风险，drop 必须重新键入完整 ID，页面不声称会删除远端数据。
- 根页面仅接受 GET/HEAD，未知资源 404，并设置 no-store、CSP、no-referrer、nosniff 和
  frame deny。关闭 UI 后根路由 404，原 `/status` 路由输出未改变；非回环 control listener
  仍在绑定前拒绝。
- 浏览器 mutation 只放行 Origin 与 Host 精确一致且 Fetch-Site 为 same-origin 的请求；
  跨站 Origin、DNS rebinding Host、cross-site fetch 及缺少专用 header 的请求仍拒绝。
  config/control/CLI 相关测试五轮 race 通过，HTML tidy 无错误，gofmt 干净。本地浏览器
  没有可用实例，未完成截图、窄屏和交互视觉验收；Web 账号/授权入口仍开放。
- 收尾全库普通测试、vet、gofmt 检查通过；最终
  `go test -race ./... -count=1 -timeout 300s` 通过（meta 141.365s，VFS 45.652s）。

## 2026-09-05 第三十九批：只读 WebDAV VFS 输出

- 新增 `internal/webdavsrv`，将一个规范绝对 `webdav.root` 映射为配置 prefix 下的 DAV
  根。FileSystem 只使用 VFS StatPath/ReadDirPath/Open/Read/Release；GET 持有并关闭真实
  VFS handle，未直接访问 provider、cache 路径或元数据库。
- OPTIONS 只公布 OPTIONS/PROPFIND/GET/HEAD；PROPFIND 限 Depth 0/1 与 64 KiB body，
  infinity 和所有写/锁/移动方法拒绝。Range 由标准 ServeContent 处理；ETag 对 remote +
  provider version 做 SHA-256 截断，保持条件读取稳定性而不泄露 opaque version。
- root 映射在 clean 后拼接，反斜杠/NUL 拒绝。非回环地址在 bind 前要求至少 16 字节
  `CLOUDFS_WEBDAV_TOKEN`，换行/NUL/超长 token 拒绝；bearer 与 Basic `cloudfs` 都使用
  constant-time 比较。YAML/argv 不保存 token，文档明确内建 HTTP 跨机需 TLS/VPN。
- WebDAV 与 mount、MCP-only owner 及 Docker 8080 映射已接线；任一 listener 同步 bind
  失败会中止启动，MCP stdio 不写额外提示。配置支持 `http/prefix/root`，省略 http 即关闭。
- HTTP 五轮 race 回放覆盖 bearer/Basic、Range、PROPFIND、OPTIONS、只读拒绝、无限深度、
  root confinement、opaque ETag 与 handle release。无 FUSE 的 assembled daemon→VFS→
  fake provider 端到端五轮 race 验证首次 WebDAV 读取经过 provider、热读零远端调用。
- rclone/cadaver 未安装，本机也无 Docker daemon/真实媒体客户端；可写 DAV、redirect/auto、
  大文件长播、TLS proxy 和 NAS 环境仍未验收，本项不标为完整。
- 收尾全库普通测试、vet、gofmt、native 与 Linux amd64 静态构建通过；最终
  `go test -race ./... -count=1 -timeout 300s` 通过（meta 133.823s，VFS 43.760s，
  webdavsrv 4.199s）。

## 2026-09-05 第四十批：WebDAV 直链策略

- `webdav.strategy` 增加 proxy（默认）、redirect、auto。GET/HEAD 先按固定 DAV root
  映射回 VFS path，再调用 VFS DownloadURL；适配器仍不解析 provider。目录继续由 DAV
  handler 处理。
- 只允许无 userinfo、无 CR/LF、长度有界的 http(s) Location，302 响应为 private/no-store。
  provider link 带任何 UA/Referer/header 时不重定向，CloudFS 的 bearer/Basic 认证也绝不
  拼入或转发到 Location。
- auto 对 header-bound link、provider/本地待上传错误和非法 URL 回落已有 VFS proxy；
  redirect 对同样情形返回通用 502，不输出 provider 错误或一个必然 403 的直链。prefix
  使用路径段边界匹配，`/davish` 不能借 `/dav` 配置触发 direct link。
- config/WebDAV/e2e/CLI 五轮 race 回归通过；单测覆盖纯 302、百度式 UA header 回落、
  强制 redirect 拒绝及 javascript/userinfo/scheme-relative/CRLF URL。真实 CDN 过期、媒体
  客户端 follow redirect 与 provider 账号仍待验收。
- 相关包扩大到十轮 race 仍通过；收尾全库普通测试、vet、gofmt、native/Linux amd64
  静态构建通过。最终 `go test -race ./... -count=1 -timeout 300s` 通过（meta 140.522s，
  VFS 49.067s，webdavsrv 4.122s）。

## 2026-09-05 第四十一批：有界 STRM 生成器

- 新增 `cloudfs strm <virtual-path> --out <dir>`。命令只读取配置并请求正在运行的 WebDAV，
  不构建 daemon、不打开 journal/meta/provider；监听通配地址会转换为本机 127.0.0.1，
  `--base-url` 可指定 reverse proxy 下的 DAV root。
- 生成器用 Depth-1 PROPFIND 递归；只接受同源、起始 DAV 根内的当前目录直接子项，过滤
  非成功 propstat 和非媒体扩展名。目录深度、媒体数与单响应大小有硬上限，响应排序后
  生成，大小写不敏感的 `.strm` 目标碰撞明确失败。
- 输出根和子目录拒绝 symlink/路径逃逸；文件经同目录临时文件、chmod、fsync、rename 和
  目录 fsync 原子落地，相同内容计为 unchanged 而不重写。默认不自动删除陈旧文件，避免
  短暂远端缺失造成媒体库破坏。
- PROPFIND 可用环境 token 做 Basic Auth；生成 URL 默认无凭据。仅显式
  `--embed-basic-auth` 才把用户 `cloudfs` 与 token 写入 URL，以 0600 落盘并输出明文凭据
  告警。非成功 HTTP 响应不回显不可信 body。
- 单测覆盖递归目录、带空格 URL、认证、二次幂等、跨域/越根/非直接子项过滤、输出碰撞、
  深度/文件上限和 symlink 逃逸。真实媒体客户端、十万级 provider 调用上界仍开放，
  T-20 保持部分完成。
- 命令级回放穿过配置加载、参数解析、HTTP 枚举与实际文件落盘；相关包十轮 race 通过。
  收尾全库普通测试、vet、gofmt、原生构建与 Linux amd64 静态构建通过；最终
  `go test -race ./... -count=1 -timeout 300s` 通过（meta 135.086s，VFS 40.982s，
  strmgen 3.828s）。

## 2026-09-05 第四十二批：跳播取消旧 read-ahead

- 审计确认已有读路径会用 16 KiB 子块服务随机 Range，并在连续访问后把窗口从 1 块增长到
  16 块；缺陷是 detached read-ahead 只受两分钟 timeout 控制，播放器 seek 或关闭句柄后
  仍可能下载旧播放位置后面的整块。
- 每个打开句柄现在为当前顺序段持有独立 context。新 read 在等待目标范围前判断 seek 并
  取消旧段；Release 同样取消。最后一个在途读取若与 close 竞争，完成后不能重新创建预读。
- 当前台正好读取被旧预读占用的块时，singleflight 可能以后台 cancellation 完成；前台在
  自己的 context 仍有效时重试一次，播放器不会收到伪造的 `context canceled`。
- 新回归先让两个旧位置预读真实进入带延迟 provider，再跳到其中一块及文件 80% 处；断言
  前台同块读取成功、旧请求不计入传输、缓存/传输仅含四个已用整块加目标子块。另覆盖
  Release 与最后一次 arm read 的竞争，close 后不产生 provider 请求。顺序/随机/seek/
  close 专项在 race 下连续二十轮通过。收尾全库普通测试、vet、gofmt 与构建通过；最终
  `go test -race ./... -count=1 -timeout 300s` 通过（meta 134.201s，VFS 42.621s）。真实
  媒体客户端和 provider 环境仍待验收。

## 2026-09-05 第四十三批：manifest 约束的 STRM prune

- `cloudfs strm --prune` 维护版本化、绑定规范 StartURL 的 0600 manifest。首次启用没有删除
  候选，只建立基线；源 URL、版本、JSON、相对路径或 SHA-256 格式不匹配时在枚举/删除前
  失败，避免复用输出目录时误清另一棵媒体库。
- 后续成功完整枚举后，仅考虑旧 manifest 存在而新集合不存在的条目。目标的每层父目录和
  文件必须仍为非 symlink 的真实目录/普通文件，内容摘要必须仍等于上次生成值；修改项保留
  并从新 manifest 移交为用户文件，缺失项忽略，不删除目录和任何未登记文件。
- 删除后的父目录先 fsync，再原子写新 manifest；原子替换现在不再吞掉目录打开/fsync
  错误。含凭据输出或 manifest 内容未变但权限被放宽时，会重新收紧并 fsync 到 0600。
- prune/认证/生成器与 CLI 专项在 race 下连续二十轮通过。收尾全库普通测试、vet、gofmt、
  原生构建及 Linux amd64 静态构建通过；最终 `go test -race ./... -count=1 -timeout 300s`
  通过（meta 133.106s，VFS 44.185s，strmgen 3.888s）。

## 2026-09-05 第四十四批：S3 / 兼容对象存储

- 新增 `internal/provider/s3` 并注册 `type: s3`。MinIO Go SDK v7.3.0 负责 SigV4、AWS
  streaming payload、multipart 与预签名，未引入完整 rclone 后端树。
- 新增 `httpx.RawTransport`：SDK 保留原始 status/header/body 做 S3 XML 解析，同时请求仍
  经过现有 proxy transport、三类 limiter 与 breaker；非 2xx 用无查询 URL 反馈限流状态，
  不泄露签名。SDK MaxRetries=1，避免和 journal/uploader 双重重放流式 body。
- 可选 prefix 把所有规范 ID 限制在一棵 key 命名空间；ListObjectsV2 delimiter 通过带
  context channel 流式交付，visitor 错误会取消并排空。目录 marker/虚拟目录、ETag 条件
  Range、仅可靠单段 ETag 的 MD5、15 分钟预签名均有明确映射。
- 小文件单 PUT；大文件 session 持久化 upload ID/key/size，part token 使用 0→1 基索引
  转换且完成前验证连续唯一，新的 Provider 实例可继续旧 session。目录/文件改名与移动
  先服务端 copy、全部完成后删源；复制失败尽力回滚目标，根删除拒绝，树操作上限一百万。
- 状态化 S3 HTTP 回放验证实际 SigV4 Authorization 并解码 AWS streaming chunks，覆盖
  列举/Stat/Range/完整读/预签名/单 PUT/multipart 恢复/Copy/文件及目录移动删除/错误映射；
  Factory→共享 HTTP transport→VFS 冷读也已通过。专项 race 二十轮通过。真实 AWS、MinIO、
  R2、版本化 bucket、Object Lock 和外部并发仍未验收，T-02 保持部分完成。
- 收尾全库普通测试、vet、gofmt、原生构建与 Linux amd64、darwin arm64 无 CGO 构建通过；
  最终 `go test -race ./... -count=1 -timeout 300s` 通过（meta 131.301s，VFS 42.705s，
  S3 3.959s，httpx 4.071s）。

## 2026-09-05 第四十五批：Dropbox 与不可回卷 HTTP body

- 新增 `internal/provider/dropbox` 并注册 `type: dropbox`，直接使用官方 HTTP API v2 而不引入
  完整 rclone 树。`path_lower` 是路径身份，文件 `rev` 是 cache 版本；根 Stat 合成，服务端
  name/path、direct child、控制字符和路径规范均在发布给 VFS 前校验。
- `list_folder` / `continue` 保留 cursor 分页并实现 `StreamLister`；响应 JSON 有 16 MiB 上限。
  download 使用 ASCII 转义的 `Dropbox-API-Arg`，同时接受 200/206，但在 Range body 进入 cache
  前核对响应 metadata 路径和 rev。临时链接的 metadata 和 HTTP(S) URL 也先验证，TTL 为四小时。
- 64 MiB 内单次 upload；大文件 session 把 session id/target/size/part size 持久化，顺序 part
  可由新 Provider 实例恢复。part 长度、连续性和 token 全部校验且不修改输入切片；重复 append
  仅在 correct offset 精确等于本 part 末尾时确认。finish 失败不以同大小旧目标猜测成功。
- create/copy/move/delete 使用 v2 服务端接口，根操作和自嵌套 move 拒绝。access token 可直接
  导入；offline refresh token + client id（可选机密 client secret）支持过期前/401 后刷新，
  并发请求复用刷新结果，短期 token 只留内存。
- 修复共享 `httpx` 的流式重试契约：无 `GetBody` 的 body 强制单次尝试；有 `GetBody` 时每轮真正
  调用它重建 reader。此前 5xx 后可能把已消费 reader 作为空/截断 body 再发。两种路径均有
  回归测试。
- 状态化 Dropbox HTTP 回放覆盖分页/visitor 停止、Stat、Range/revision、临时链接、Unicode、
  单次上传、跨实例 session、重复 part、Copy/Rename/Move/Delete、错误映射、token refresh、
  Factory 共享 client 与 VFS 冷读。Dropbox/httpx 专项 race 二十轮通过；全库普通测试、vet、
  原生构建及 Linux amd64、darwin arm64 无 CGO 构建通过；最终全库 race 通过（meta 129.755s、
  VFS 44.779s、Dropbox 4.014s、httpx 5.006s）。真实 Dropbox 账号、changes delta、共享
  namespace、团队 Select-User 与浏览器 OAuth 尚未验收。

## 2026-09-05 第四十六批：Dropbox changes 与 cursor reset

- Dropbox 实现 `ChangeLister` 并声明 `Caps.Delta`。首次 poll 调用
  `list_folder/get_latest_cursor` 建立 account-wide recursive 基线，不回放整个账号；首次基线
  也用显式 `CursorResetError` 通知 VFS，使可能来自停机前的持久目录缓存先失效再保存 cursor。
- 后续 `list_folder/continue` 将 file/folder metadata 映射为 upsert，将 deleted metadata 的规范
  路径映射为 delete；逐项校验 path/name/parent/rev。每次推进服务端返回的新 cursor，has_more
  页面由后续 poll 继续，不把普通目录分页 cursor 和 remote delta cursor 混用。
- Provider 新增通用 `ErrCursorReset` / `CursorResetError{Cursor}` 契约。Dropbox 收到官方 reset
  后先取得新基线再返回该错误；VFS 仅在 replacement cursor 非空且不同于旧值时处理，先调用
  `InvalidateAll`，再持久化新 cursor，并向订阅者和内核发全局 rescan。崩溃在两步之间只会
  留下 stale listing 并在下次重试，不会跳过 invalidation；节点、pending write 和文件块不删。
- 状态化 HTTP 测试覆盖首次基线、revision upsert、path delete、失效 cursor 换代；Dropbox
  Factory→VFS 测试从已列举目录建立基线、重新列举，再应用外部修改并读取新内容。通用 VFS
  回归验证 listing 失效、cursor 保存、rescan、统计及相同 replacement 拒绝。Dropbox/VFS/
  provider 专项 race 三轮通过（VFS 106.097s）；全库普通测试、vet、原生构建及 Linux amd64、
  darwin arm64 无 CGO 构建通过；最终全库 race 通过（meta 134.254s、VFS 43.832s、Dropbox
  4.273s）。真实 namespace/reset 尚待账号环境验证。

## 2026-09-05 第四十七批：OneDrive / Microsoft Graph

- 新增 `internal/provider/onedrive` 并注册 `type: onedrive`，直接调用 Microsoft Graph v1.0。
  Graph stable `DriveItem.id` 是对象身份，文件版本优先使用 `cTag`、退回 `eTag`，合法 SHA-1
  纳入能力矩阵；根的真实 item ID 延迟解析。服务端 ID、名称、父目录、类型、大小、时间和
  continuation URL 都在进入 VFS 前验证。
- children 保留 `@odata.nextLink` 分页并实现 visitor 流式列举，限制同 origin/API 基路径、
  重复游标和一万页上限。delta 首次用 `token=latest` 建立基线并触发通用 cursor reset 目录
  失效；后续 stable-ID upsert/delete 和 410/`resyncRequired` 换基线已接入。
- Range 在访问预认证 `@microsoft.graph.downloadUrl` 前核对内容版本，CDN URL 绝不携带
  Graph Bearer。链接按 item/version 只在内存缓存十分钟，避免 VFS 每块额外一次元数据请求；
  403/410 后丢弃缓存、刷新元数据并再次核对版本。对外链接保守声明十五分钟，任何 HTTP
  status URL 已由共享 `httpx` 统一移除 query/fragment，错误不会泄露签名。
- 小文件使用单次 content PUT；大文件 upload session 的预认证 URL、目标、大小和 320 KiB
  对齐 part size 持久化到 journal，顺序 fragment 单请求严格小于 60 MiB、文件上限 250 GB，
  新 Provider 可续传。每页偏移确认、末片 item ID、part 唯一连续性与提交后 Stat 均验证。
  原生 Mkdir/Move/Rename/Delete 已接入；Graph 无通用同步 Copy，因此如实声明 ServerCopy=false。
- access token 与 offline refresh token 都支持；刷新轮换值通过通用 token persistence 先落盘，
  并发调用合并。状态化 Graph/CDN 回放覆盖分页停止、Range 缓存/过期/版本冲突、Unicode、跨
  Provider session、操作、错误映射、轮换持久化、delta/reset，以及 Factory→VFS 冷读和增量
  更新。OneDrive/httpx 专项 race 二十轮通过。
- 全库普通测试、vet、gofmt、原生构建及 Linux amd64、darwin arm64 无 CGO 构建通过；最终
  `go test -race ./... -count=1 -timeout 300s` 通过（meta 130.775s、VFS 45.097s、OneDrive
  5.873s、httpx 6.826s）。真实个人/组织/SharePoint/国家云账号、浏览器 OAuth、长期 throttling
  和 250 GB 边界尚未验收，T-02 保持部分完成。
